id: "http://example.org/urlimport"
title: Base for import test
name: inheritedslots
description: A test to make sure that "same as" appears as a slot in named thing

prefixes:
  meta: https://w3id.org/linkml/meta/

default_prefix: meta
default_range: string

imports:
  - meta:meta

