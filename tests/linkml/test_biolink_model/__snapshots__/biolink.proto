syntax = "proto3";
package biolink_model;
// metamodel_version: 1.11.0
// version: 4.2.5
// A region (or regions) of a chromatinized genome that has been measured to be more accessible to an enzyme such as DNase-I or Tn5 Transpose
message AccessibleDnaRegion {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 15;
}
// An activity is something that occurs over a period of time and acts upon or with entities; it may include consuming, processing, transforming, modifying, relocating, using, or generating entities.
message Activity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// Activity or behavior of any independent integral living, organization or mechanical actor in the world
message ActivityAndBehavior {
}
message AdministrativeEntity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// person, group, organization or project that provides a piece of information (i.e. a knowledge association)
message Agent {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // a professional relationship between one provider (often a person) within another provider (often an organization). Target provider identity should be specified by a CURIE. Providers may have multiple affiliations.
  repeated string affiliation = 11;
  // the particulars of the place where someone or an organization is situated.  For now, this slot is a simple text "blob" containing all relevant details of the given location for fitness of purpose. For the moment, this "address" can include other contact details such as email and phone number(?).
  string address = 12;
  // Different classes of agents have distinct preferred identifiers. For publishers, use the ISBN publisher code. See https://grp.isbn-international.org/ for publisher code lookups. For editors, authors and  individual providers, use the individual's ORCID if available; Otherwise, a ScopusID, ResearchID or Google Scholar ID ('GSID') may be used if the author ORCID is unknown. Institutional agents could be identified by an International Standard Name Identifier ('ISNI') code.
  string id = 13;
  // it is recommended that an author's 'name' property be formatted as "surname, firstname initial."
  string name = 14;
}
// A subcellular location, cell type or gross anatomical part
message AnatomicalEntity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
message AnatomicalEntityToAnatomicalEntityAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  AnatomicalEntity subject = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  AnatomicalEntity object = 39;
}
// A relationship between two anatomical entities where the relationship is ontogenic, i.e. the two entities are related by development. A number of different relationship types can be used to specify the precise nature of the relationship.
message AnatomicalEntityToAnatomicalEntityOntogenicAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the structure at a later time
  AnatomicalEntity subject = 37;
  // the structure at an earlier time
  AnatomicalEntity object = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
}
// A relationship between two anatomical entities where the relationship is mereological, i.e the two entities are related by parthood. This includes relationships between cellular components and cells, between cells and tissues, tissues and whole organisms
message AnatomicalEntityToAnatomicalEntityPartOfAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the part
  AnatomicalEntity subject = 37;
  // the whole
  AnatomicalEntity object = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
}
// Biolink Model root class for entity annotations.
message Annotation {
}
// a piece of writing on a particular topic presented as a stand-alone section of a larger publication
message Article {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // a long-form human readable name for a thing
  string full_name = 7;
  // Alternate human-readable names for a thing
  repeated string synonym = 8;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 9;
  string license = 10;
  string rights = 11;
  string format = 12;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 13;
  // connects an publication to the list of authors who contributed to the publication. This property should be a comma-delimited list of author names. It is recommended that an author's name be formatted as "surname, firstname initial.".   Note that this property is a node annotation expressing the citation list of authorship which might typically otherwise be more completely documented in biolink:PublicationToProviderAssociation defined edges which point to full details about an author and possibly, some qualifiers which clarify the specific status of a given author in the publication.
  repeated Agent authors = 14;
  // When a 2-tuple of page numbers are provided, they represent the start and end page of the publication within its parent publication context. For books, this may be set to the total number of pages of the book.
  repeated string pages = 15;
  // executive  summary of a publication
  string summary = 16;
  // keywords tagging a publication
  repeated string keywords = 17;
  // mesh terms tagging a publication
  repeated string mesh_terms = 18;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 19;
  // Ontology term for publication type may be drawn from Dublin Core types (https://www.dublincore.org/specifications/dublin-core/dcmi-type-vocabulary/), FRBR-aligned Bibliographic Ontology (https://sparontologies.github.io/fabio/current/fabio.html), the MESH publication types (https://www.nlm.nih.gov/mesh/pubtypes.html), the Confederation of Open Access Repositories (COAR) Controlled Vocabulary for Resource Type Genres (http://vocabularies.coar-repositories.org/documentation/resource_types/), Wikidata (https://www.wikidata.org/wiki/Wikidata:Publication_types), or equivalent publication type ontology. When a given publication type ontology term is used within a given knowledge graph, then the CURIE identified term must be documented in the graph as a concept node of biolink:category biolink:OntologyClass.
  repeated string publication_type = 20;
  // Different kinds of publication subtypes will have different preferred identifiers (curies when feasible). Precedence of identifiers for scientific articles is as follows: PMID if available; DOI if not; actual alternate CURIE otherwise. Enclosing publications (i.e. referenced by 'published in' node property) such as books and journals, should have industry-standard identifier such as from ISBN and ISSN.
  string id = 21;
  // the 'title' of the publication is generally recorded in the 'name' property (inherited from NamedThing). The field name 'title' is now also tagged as an acceptable alias for the node property 'name' (just in case).
  string name = 22;
  // The enclosing parent serial containing the article should have industry-standard identifier from ISSN.
  string published_in = 23;
  // Optional value, if used locally as a convenience, is set to the iso abbreviation of the 'published in' parent.
  string iso_abbreviation = 24;
  // volume of a book or music release in a collection/series or a published collection of journal issues in a serial publication
  string volume = 25;
  // issue of a newspaper, a scientific journal or magazine for reference purpose
  string issue = 26;
}
// A typed association between two entities, supported by evidence
message Association {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 9;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 10;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 11;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 12;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 13;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 14;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 15;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 16;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 17;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 18;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 19;
  // a point in time
  string timepoint = 20;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 21;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 22;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 24;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 25;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 26;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 27;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 28;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 29;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 30;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 31;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 32;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 33;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 34;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 35;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 36;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 37;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 38;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 39;
}
// A property or characteristic of an entity. For example, an apple may have properties such as color, shape, age, crispiness. An environmental sample may have attributes such as depth, lat, long, material.
message Attribute {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// A member of a group of unicellular microorganisms lacking a nuclear membrane, that reproduce by binary fission and are often motile.
message Bacterium {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
message Behavior {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // holds between a process and a continuant, where the continuant is an input into the process
  repeated NamedThing has_input = 15;
  // holds between a process and a continuant, where the continuant is an output of the process
  repeated NamedThing has_output = 16;
  // holds between a process and a physical entity, where the physical entity executes the process
  repeated PhysicalEntity enabled_by = 17;
}
// An association between an mixture behavior and a behavioral feature manifested by the individual exhibited or has exhibited the behavior.
message BehaviorToBehavioralFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // behavior that is the subject of the association
  Behavior subject = 38;
  // behavioral feature that is the object of the association
  BehavioralFeature object = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 46;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 47;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 48;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 49;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 50;
  // number of things with a particular property
  int32 has_count = 51;
  // total number of things in a particular reference set
  int32 has_total = 52;
  double has_quotient = 53;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 54;
}
// A behavioral exposure is a factor relating to behavior impacting an individual.
message BehavioralExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
  // a point in time
  string timepoint = 16;
}
// A phenotypic feature which is behavioral in nature.
message BehavioralFeature {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// An outcome resulting from an exposure event which is the manifestation of human behavior.
message BehavioralOutcome {
}
message BiologicalEntity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// One or more causally connected executions of molecular functions
message BiologicalProcess {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // holds between a process and a continuant, where the continuant is an input into the process
  repeated NamedThing has_input = 15;
  // holds between a process and a continuant, where the continuant is an output of the process
  repeated NamedThing has_output = 16;
  // holds between a process and a physical entity, where the physical entity executes the process
  repeated PhysicalEntity enabled_by = 17;
}
// Either an individual molecular activity, or a collection of causally connected molecular activities in a biological system.
message BiologicalProcessOrActivity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // holds between a process and a continuant, where the continuant is an input into the process
  repeated NamedThing has_input = 15;
  // holds between a process and a continuant, where the continuant is an output of the process
  repeated NamedThing has_output = 16;
  // holds between a process and a physical entity, where the physical entity executes the process
  repeated PhysicalEntity enabled_by = 17;
}
message BiologicalSex {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// An external biotic exposure is an intake of (sometimes pathological) biological organisms (including viruses).
message BioticExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
  // a point in time
  string timepoint = 16;
}
// This class may rarely be instantiated except if use cases of a given knowledge graph support its utility.
message Book {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  // a human-readable description of an entity
  string description = 2;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 3;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 4;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 5;
  // a long-form human readable name for a thing
  string full_name = 6;
  // Alternate human-readable names for a thing
  repeated string synonym = 7;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 8;
  string license = 9;
  string rights = 10;
  string format = 11;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 12;
  // connects an publication to the list of authors who contributed to the publication. This property should be a comma-delimited list of author names. It is recommended that an author's name be formatted as "surname, firstname initial.".   Note that this property is a node annotation expressing the citation list of authorship which might typically otherwise be more completely documented in biolink:PublicationToProviderAssociation defined edges which point to full details about an author and possibly, some qualifiers which clarify the specific status of a given author in the publication.
  repeated Agent authors = 13;
  // When a 2-tuple of page numbers are provided, they represent the start and end page of the publication within its parent publication context. For books, this may be set to the total number of pages of the book.
  repeated string pages = 14;
  // executive  summary of a publication
  string summary = 15;
  // keywords tagging a publication
  repeated string keywords = 16;
  // mesh terms tagging a publication
  repeated string mesh_terms = 17;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 18;
  // Ontology term for publication type may be drawn from Dublin Core types (https://www.dublincore.org/specifications/dublin-core/dcmi-type-vocabulary/), FRBR-aligned Bibliographic Ontology (https://sparontologies.github.io/fabio/current/fabio.html), the MESH publication types (https://www.nlm.nih.gov/mesh/pubtypes.html), the Confederation of Open Access Repositories (COAR) Controlled Vocabulary for Resource Type Genres (http://vocabularies.coar-repositories.org/documentation/resource_types/), Wikidata (https://www.wikidata.org/wiki/Wikidata:Publication_types), or equivalent publication type ontology. When a given publication type ontology term is used within a given knowledge graph, then the CURIE identified term must be documented in the graph as a concept node of biolink:category biolink:OntologyClass.
  repeated string publication_type = 19;
  // the 'title' of the publication is generally recorded in the 'name' property (inherited from NamedThing). The field name 'title' is now also tagged as an acceptable alias for the node property 'name' (just in case).
  string name = 20;
  // Books should have industry-standard identifier such as from ISBN.
  string id = 21;
  // Should generally be set to an ontology class defined term for 'book'.
  repeated string type = 22;
}
message BookChapter {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // a long-form human readable name for a thing
  string full_name = 7;
  // Alternate human-readable names for a thing
  repeated string synonym = 8;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 9;
  string license = 10;
  string rights = 11;
  string format = 12;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 13;
  // connects an publication to the list of authors who contributed to the publication. This property should be a comma-delimited list of author names. It is recommended that an author's name be formatted as "surname, firstname initial.".   Note that this property is a node annotation expressing the citation list of authorship which might typically otherwise be more completely documented in biolink:PublicationToProviderAssociation defined edges which point to full details about an author and possibly, some qualifiers which clarify the specific status of a given author in the publication.
  repeated Agent authors = 14;
  // When a 2-tuple of page numbers are provided, they represent the start and end page of the publication within its parent publication context. For books, this may be set to the total number of pages of the book.
  repeated string pages = 15;
  // executive  summary of a publication
  string summary = 16;
  // keywords tagging a publication
  repeated string keywords = 17;
  // mesh terms tagging a publication
  repeated string mesh_terms = 18;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 19;
  // Ontology term for publication type may be drawn from Dublin Core types (https://www.dublincore.org/specifications/dublin-core/dcmi-type-vocabulary/), FRBR-aligned Bibliographic Ontology (https://sparontologies.github.io/fabio/current/fabio.html), the MESH publication types (https://www.nlm.nih.gov/mesh/pubtypes.html), the Confederation of Open Access Repositories (COAR) Controlled Vocabulary for Resource Type Genres (http://vocabularies.coar-repositories.org/documentation/resource_types/), Wikidata (https://www.wikidata.org/wiki/Wikidata:Publication_types), or equivalent publication type ontology. When a given publication type ontology term is used within a given knowledge graph, then the CURIE identified term must be documented in the graph as a concept node of biolink:category biolink:OntologyClass.
  repeated string publication_type = 20;
  // Different kinds of publication subtypes will have different preferred identifiers (curies when feasible). Precedence of identifiers for scientific articles is as follows: PMID if available; DOI if not; actual alternate CURIE otherwise. Enclosing publications (i.e. referenced by 'published in' node property) such as books and journals, should have industry-standard identifier such as from ISBN and ISSN.
  string id = 21;
  // the 'title' of the publication is generally recorded in the 'name' property (inherited from NamedThing). The field name 'title' is now also tagged as an acceptable alias for the node property 'name' (just in case).
  string name = 22;
  // The enclosing parent book containing the chapter should have industry-standard identifier from ISBN.
  string published_in = 23;
  // volume of a book or music release in a collection/series or a published collection of journal issues in a serial publication
  string volume = 24;
  // chapter of a book
  string chapter = 25;
}
// An individual (human) organism that has a patient role in some clinical context.
message Case {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// An abstract association for use where the case is the subject
message CaseToEntityAssociationMixin {
  // the case (e.g. patient) that has the property
  Case subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
// An association between a case (e.g. individual patient) and a phenotypic feature in which the individual has or has had the phenotype.
message CaseToPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 9;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 10;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 11;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 12;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 13;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 14;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 15;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 16;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 17;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 18;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 19;
  // a point in time
  string timepoint = 20;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 21;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 22;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 24;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 25;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 26;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 27;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 28;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 29;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 30;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 31;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 32;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 33;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 34;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 35;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 36;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 37;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 38;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 46;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 47;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 48;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 49;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 50;
  // number of things with a particular property
  int32 has_count = 51;
  // total number of things in a particular reference set
  int32 has_total = 52;
  double has_quotient = 53;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 54;
}
message CausalGeneToDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum subject_aspect_qualifier = 37;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 41;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 42;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 43;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 44;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 45;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 46;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 47;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 48;
  // number of things with a particular property
  int32 has_count = 49;
  // total number of things in a particular reference set
  int32 has_total = 50;
  double has_quotient = 51;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 52;
  // gene in which variation is shown to cause the disease.
  GeneOrGeneProduct subject = 53;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Disease object = 54;
}
message Cell {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
message CellLine {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
message CellLineAsAModelOfDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 8;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 9;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 10;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 11;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 12;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 13;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 14;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 15;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 16;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 17;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 18;
  // a point in time
  string timepoint = 19;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 20;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 21;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 24;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 25;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 26;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 27;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 28;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 29;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 30;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 31;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 32;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 33;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 34;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 35;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 36;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 37;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 38;
  // A cell line derived from an organismal entity with a disease state that is used as a model of that disease.
  CellLine subject = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 46;
}
// An relationship between a cell line and a disease or a phenotype, where the cell line is derived from an individual with that disease or phenotype.
message CellLineToDiseaseOrPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 8;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 9;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 10;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 11;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 12;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 13;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 14;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 15;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 16;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 17;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 18;
  // a point in time
  string timepoint = 19;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 20;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 21;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 24;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 25;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 26;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 27;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 28;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 29;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 30;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 31;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 32;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 33;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 34;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 35;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 36;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 37;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 38;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  DiseaseOrPhenotypicFeature subject = 39;
}
// An relationship between a cell line and another entity
message CellLineToEntityAssociationMixin {
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  CellLine subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
// A location in or around a cell
message CellularComponent {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
message CellularOrganism {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// Describes an effect that a chemical has on a gene or gene product (e.g. an impact of on its abundance, activity,localization, processing, expression, etc.)
message ChemicalAffectsGeneAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // A qualifier that composes with a core subject/object concept to define a specific type, variant, alternative version of this concept. The composed concept remains a subtype or instance of the core concept. For example, the qualifier ‘mutation’ combines with the core concept ‘Gene X’ to express the compose concept ‘a mutation of Gene X’.  This qualifier specifies a change in the subject of an association (aka: statement).
  ChemicalOrGeneOrGeneProductFormOrVariantEnum subject_form_or_variant_qualifier = 37;
  // defines a specific part/component of the core concept (used in cases there this specific part has no IRI we can use to directly represent it, e.g. 'ESR1 transcript' q: polyA tail).  This qualifier is for the  subject of an association (or statement).
  GeneOrGeneProductOrChemicalPartQualifierEnum subject_part_qualifier = 38;
  // A qualifier that composes with a core subject/object  concept to describe something that is derived from the core concept.  For example, the qualifier ‘metabolite’ combines with a ‘Chemical X’ core concept to express the composed concept ‘a metabolite of Chemical X’.  This qualifier is for the subject of an association  (or statement).
  ChemicalEntityDerivativeEnum subject_derivative_qualifier = 39;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum subject_aspect_qualifier = 40;
  AnatomicalEntity subject_context_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // A qualifier that composes with a core subject/object concept to define a specific type, variant, alternative version of this concept. The composed concept remains a subtype or instance of the core concept. For example, the qualifier ‘mutation’ combines with the core concept ‘Gene X’ to express the compose concept ‘a mutation of Gene X’.  This qualifier specifies a change in the object of an association (aka: statement).
  ChemicalOrGeneOrGeneProductFormOrVariantEnum object_form_or_variant_qualifier = 43;
  // defines a specific part/component of the core concept (used in cases there this specific part has no IRI we can use to directly represent it, e.g. 'ESR1 transcript' q: polyA tail).  This qualifier is for the  object of an association (or statement).
  GeneOrGeneProductOrChemicalPartQualifierEnum object_part_qualifier = 44;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum object_aspect_qualifier = 45;
  AnatomicalEntity object_context_qualifier = 46;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 47;
  // A statement qualifier representing a type of molecular control mechanism through which an effect of a chemical on a gene or gene product is mediated (e.g. 'agonism', 'inhibition', 'allosteric modulation', 'channel blocker')
  CausalMechanismQualifierEnum causal_mechanism_qualifier = 48;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  AnatomicalEntity anatomical_context_qualifier = 49;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 50;
  // A statement qualifier representing a taxonomic category of species in which a relationship expressed in an association took place.
  OrganismTaxon species_context_qualifier = 51;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  ChemicalEntity subject = 52;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 53;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  GeneOrGeneProduct object = 54;
}
// A chemical entity is a physical entity that pertains to chemistry or biochemistry.
message ChemicalEntity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
}
message ChemicalEntityAssessesNamedThingAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  ChemicalEntity subject = 37;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
}
// A union of chemical entities and children, and gene or gene product. This mixin is helpful to use when searching across chemical entities that must include genes and their children as chemical entities.
message ChemicalEntityOrGeneOrGeneProduct {
}
// A regulatory relationship between two genes
message ChemicalEntityOrGeneOrGeneProductRegulatesGeneAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 37;
  // the direction is always from regulator to regulated
  string predicate = 38;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  ChemicalEntityOrGeneOrGeneProduct subject = 39;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  GeneOrGeneProduct object = 40;
}
// A union of chemical entities and children, and protein and polypeptide. This mixin is helpful to use when searching across chemical entities that must include genes and their children as chemical entities.
message ChemicalEntityOrProteinOrPolypeptide {
}
// An interaction between a chemical entity and another entity
message ChemicalEntityToEntityAssociationMixin {
  // the chemical entity that is an interactor
  ChemicalEntityOrGeneOrGeneProduct subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
// A chemical exposure is an intake of a particular chemical entity.
message ChemicalExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 13;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 14;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 15;
  // a point in time
  string timepoint = 16;
}
// describes a physical interaction between a chemical entity and a gene or gene product. Any biological or chemical effect resulting from such an interaction are out of scope, and covered by the ChemicalAffectsGeneAssociation type (e.g. impact of a chemical on the abundance, activity, structure, etc, of either participant in the interaction)
message ChemicalGeneInteractionAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // A qualifier that composes with a core subject/object concept to define a specific type, variant, alternative version of this concept. The composed concept remains a subtype or instance of the core concept. For example, the qualifier ‘mutation’ combines with the core concept ‘Gene X’ to express the compose concept ‘a mutation of Gene X’.  This qualifier specifies a change in the subject of an association (aka: statement).
  ChemicalOrGeneOrGeneProductFormOrVariantEnum subject_form_or_variant_qualifier = 37;
  // defines a specific part/component of the core concept (used in cases there this specific part has no IRI we can use to directly represent it, e.g. 'ESR1 transcript' q: polyA tail).  This qualifier is for the  subject of an association (or statement).
  GeneOrGeneProductOrChemicalPartQualifierEnum subject_part_qualifier = 38;
  // A qualifier that composes with a core subject/object  concept to describe something that is derived from the core concept.  For example, the qualifier ‘metabolite’ combines with a ‘Chemical X’ core concept to express the composed concept ‘a metabolite of Chemical X’.  This qualifier is for the subject of an association  (or statement).
  ChemicalEntityDerivativeEnum subject_derivative_qualifier = 39;
  AnatomicalEntity subject_context_qualifier = 40;
  // A qualifier that composes with a core subject/object concept to define a specific type, variant, alternative version of this concept. The composed concept remains a subtype or instance of the core concept. For example, the qualifier ‘mutation’ combines with the core concept ‘Gene X’ to express the compose concept ‘a mutation of Gene X’.  This qualifier specifies a change in the object of an association (aka: statement).
  ChemicalOrGeneOrGeneProductFormOrVariantEnum object_form_or_variant_qualifier = 41;
  // defines a specific part/component of the core concept (used in cases there this specific part has no IRI we can use to directly represent it, e.g. 'ESR1 transcript' q: polyA tail).  This qualifier is for the  object of an association (or statement).
  GeneOrGeneProductOrChemicalPartQualifierEnum object_part_qualifier = 42;
  AnatomicalEntity object_context_qualifier = 43;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  AnatomicalEntity anatomical_context_qualifier = 44;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  ChemicalEntity subject = 45;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  GeneOrGeneProduct object = 46;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 47;
}
// A chemical mixture is a chemical entity composed of two or more molecular entities.
message ChemicalMixture {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
  string is_supplement = 18;
  // Should be the highest level of FDA approval this chemical entity or device has, regardless of which disease, condition or phenotype it is currently being reviewed to treat.  For specific levels of FDA approval for a specific condition, disease, phenotype, etc., see the association slot, 'clinical approval status.'
  ApprovalStatusEnum highest_FDA_approval_status = 19;
  // An agglomeration of drug regulatory status worldwide. Not specific to FDA.
  ApprovalStatusEnum drug_regulatory_status_world_wide = 20;
  // the method or process of administering a pharmaceutical compound to achieve a therapeutic effect in humans or animals.
  repeated DrugDeliveryEnum routes_of_delivery = 21;
}
message ChemicalOrDrugOrTreatment {
}
// This association defines a relationship between a chemical or treatment (or procedure) and a disease or phenotypic feature where the disesae or phenotypic feature is a secondary, typically (but not always) undesirable effect.
message ChemicalOrDrugOrTreatmentSideEffectDiseaseOrPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 8;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 9;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 10;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 11;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 12;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 13;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 14;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 15;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 16;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 17;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 18;
  // a point in time
  string timepoint = 19;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 20;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 21;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 24;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 25;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 26;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 27;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 28;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 29;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 30;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 31;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 32;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 33;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 34;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 35;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 36;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 37;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 38;
  FDAIDAAdverseEventEnum FDA_adverse_event_level = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 46;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 47;
}
// This association defines a relationship between a chemical or treatment (or procedure) and a disease or phenotypic feature where the disease or phenotypic feature is a secondary undesirable effect.
message ChemicalOrDrugOrTreatmentToDiseaseOrPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 8;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 9;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 10;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 11;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 12;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 13;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 14;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 15;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 16;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 17;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 18;
  // a point in time
  string timepoint = 19;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 20;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 21;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 24;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 25;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 26;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 27;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 28;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 29;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 30;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 31;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 32;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 33;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 34;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 35;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 36;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 37;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 38;
  FDAIDAAdverseEventEnum FDA_adverse_event_level = 39;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 40;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 41;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 42;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 43;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 44;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 45;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 46;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 47;
}
// A role played by the molecular entity or part thereof within a chemical context.
message ChemicalRole {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// A relationship between two chemical entities. This can encompass actual interactions as well as temporal causal edges, e.g. one chemical converted to another.
message ChemicalToChemicalAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 37;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 38;
  // the chemical element that is the target of the statement
  ChemicalEntity object = 39;
}
// A causal relationship between two chemical entities, where the subject represents the upstream entity and the object represents the downstream. For any such association there is an implicit reaction: IF R has-input C1 AND R has-output C2 AND R enabled-by P AND R type Reaction THEN C1 derives-into C2 catalyst qualifier P
message ChemicalToChemicalDerivationAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // this connects the derivation edge to the chemical entity that catalyzes the reaction that causes the subject chemical to transform into the object chemical.
  repeated MacromolecularMachineMixin catalyst_qualifier = 37;
  // the upstream chemical entity
  ChemicalEntity subject = 38;
  // the downstream chemical entity
  ChemicalEntity object = 39;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 40;
}
// An interaction between a chemical entity and a phenotype or disease, where the presence of the chemical gives rise to or exacerbates the phenotype.
message ChemicalToDiseaseOrPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 9;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 10;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 11;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 12;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 13;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 14;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 15;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 16;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 17;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 18;
  // a point in time
  string timepoint = 19;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 20;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 21;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 24;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 25;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 26;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 27;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 28;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 29;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 30;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 31;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 32;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 33;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 34;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 35;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 36;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 37;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 38;
  // the disease or phenotype that is affected by the chemical
  DiseaseOrPhenotypicFeature object = 39;
}
// An interaction between a chemical entity and another entity
message ChemicalToEntityAssociationMixin {
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
// An interaction between a chemical entity and a biological process or pathway.
message ChemicalToPathwayAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // the chemical entity that is affecting the pathway
  ChemicalEntity subject = 38;
  // the pathway that is affected by the chemical
  Pathway object = 39;
}
// A result of a chi squared analysis.
message ChiSquaredAnalysisResult {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// Attributes relating to a clinical manifestation
message ClinicalAttribute {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// The course a disease typically takes from its onset, progression in time, and eventual resolution or death of the affected individual
message ClinicalCourse {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// Any entity or process that exists in the clinical domain and outside the biological realm. Diseases are placed under biological entities
message ClinicalEntity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// this category is currently considered broad enough to tag clinical lab measurements and other biological attributes taken as 'clinical traits' with some statistical score, for example, a p value in genetic associations.
message ClinicalFinding {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // connects any entity to an attribute
  repeated ClinicalAttribute has_attribute = 14;
}
message ClinicalIntervention {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// A clinical measurement is a special kind of attribute which results from a laboratory observation from a subject individual or sample. Measurements can be connected to their subject by the 'has attribute' slot.
message ClinicalMeasurement {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 12;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 13;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 14;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 15;
}
// Used to characterize and specify the phenotypic abnormalities defined in the phenotypic abnormality sub-ontology, with respect to severity, laterality, and other aspects
message ClinicalModifier {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
message ClinicalTrial {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
message CodingSequence {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 15;
}
// A group of people banded together or treated as a group who share common characteristics. A cohort 'study' is a particular form of longitudinal study that samples a cohort, performing a cross-section at intervals through time.
message Cohort {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// A Common Data Element (CDE) is a standardized, precisely defined question, paired with a set of allowable responses, used systematically across different sites, studies, or clinical trials to ensure consistent data collection. Multiple CDEs (from one or more Collections) can be curated into Forms. (https://cde.nlm.nih.gov/home)
message CommonDataElement {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// A complex chemical exposure is an intake of a chemical mixture (e.g. gasoline), other than a drug.
message ComplexChemicalExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// A complex molecular mixture is a chemical mixture composed of two or more molecular entities with unknown concentration and stoichiometry.
message ComplexMolecularMixture {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
  string is_supplement = 18;
  // Should be the highest level of FDA approval this chemical entity or device has, regardless of which disease, condition or phenotype it is currently being reviewed to treat.  For specific levels of FDA approval for a specific condition, disease, phenotype, etc., see the association slot, 'clinical approval status.'
  ApprovalStatusEnum highest_FDA_approval_status = 19;
  // An agglomeration of drug regulatory status worldwide. Not specific to FDA.
  ApprovalStatusEnum drug_regulatory_status_world_wide = 20;
  // the method or process of administering a pharmaceutical compound to achieve a therapeutic effect in humans or animals.
  repeated DrugDeliveryEnum routes_of_delivery = 21;
}
// A result of a concept count analysis.
message ConceptCountAnalysisResult {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// Level of confidence in a statement
message ConfidenceLevel {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// Any association between an entity (such as a publication) and various agents that contribute to its realisation
message ContributorAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 9;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 10;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 11;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 12;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 13;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 14;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 15;
  // a point in time
  string timepoint = 16;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 17;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 18;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 19;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 21;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 22;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 23;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 24;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 25;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 26;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 27;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 28;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 29;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 30;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 31;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 32;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 33;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 34;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 35;
  // information content entity which an agent has helped realise
  InformationContentEntity subject = 36;
  // generally one of the predicate values 'provider', 'publisher', 'editor' or 'author'
  string predicate = 37;
  // agent helping to realise the given entity (e.g. such as a publication)
  Agent object = 38;
  // this field can be used to annotate special characteristics of an agent relationship, such as the fact that a given author agent of a publication is the 'corresponding author'
  repeated OntologyClass qualifiers = 39;
}
message CorrelatedGeneToDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum subject_aspect_qualifier = 37;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 41;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 42;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 43;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 44;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 45;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 46;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 47;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 48;
  // number of things with a particular property
  int32 has_count = 49;
  // total number of things in a particular reference set
  int32 has_total = 50;
  double has_quotient = 51;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 52;
  // gene in which variation is shown to correlate with the disease.
  GeneOrGeneProduct subject = 53;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Disease object = 54;
}
// an item that refers to a collection of data from a data source.
message Dataset {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// an item that holds distribution level information about a dataset.
message DatasetDistribution {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
  string distribution_download_url = 17;
}
// an item that holds summary level information about a dataset.
message DatasetSummary {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
  string source_web_page = 17;
  string source_logo = 18;
}
// an item that holds version level information about a dataset.
message DatasetVersion {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
  Dataset has_dataset = 17;
  string ingest_date = 18;
  DatasetDistribution has_distribution = 19;
}
// A thing made or adapted for a particular purpose, especially a piece of mechanical or electronic equipment
message Device {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// A device or substance used to help diagnose disease or injury
message DiagnosticAid {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// A disorder of structure or function, especially one that produces specific signs, phenotypes or symptoms or that affects a specific location and is not simply a direct result of physical injury.  A disposition to undergo pathological processes that exists in an organism because of one or more disorders in that organism.
message Disease {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// Either one of a disease or an individual phenotypic feature. Some knowledge resources such as Monarch treat these as distinct, others such as MESH conflate.  Please see definitions of phenotypic feature and disease in this model for their independent descriptions.  This class is helpful to enforce domains and ranges that may involve either a disease or a phenotypic feature.
message DiseaseOrPhenotypicFeature {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// A disease or phenotypic feature state, when viewed as an exposure, represents an precondition, leading to or influencing an outcome, e.g. HIV predisposing an individual to infections; a relative deficiency of skin pigmentation predisposing an individual to skin cancer.
message DiseaseOrPhenotypicFeatureExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
  // a point in time
  string timepoint = 16;
}
// Physiological outcomes resulting from an exposure event which is the manifestation of a disease or other characteristic phenotype.
message DiseaseOrPhenotypicFeatureOutcome {
}
message DiseaseOrPhenotypicFeatureToEntityAssociationMixin {
  // disease or phenotype
  DiseaseOrPhenotypicFeature subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
// An association between either a disease or a phenotypic feature and its mode of (genetic) inheritance.
message DiseaseOrPhenotypicFeatureToGeneticInheritanceAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 38;
  // genetic inheritance associated with the specified disease or phenotypic feature.
  GeneticInheritance object = 39;
}
// An association between either a disease or a phenotypic feature and an anatomical entity, where the disease/feature manifests in that site.
message DiseaseOrPhenotypicFeatureToLocationAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 9;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 10;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 11;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 12;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 13;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 14;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 15;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 16;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 17;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 18;
  // a point in time
  string timepoint = 19;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 20;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 21;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 24;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 25;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 26;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 27;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 28;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 29;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 30;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 31;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 32;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 33;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 34;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 35;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 36;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 37;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 38;
  // anatomical entity in which the disease or feature is found.
  AnatomicalEntity object = 39;
}
message DiseaseToEntityAssociationMixin {
  // disease class
  Disease subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
// An association between an exposure event and a disease.
message DiseaseToExposureEventAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 9;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 10;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 11;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 12;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 13;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 14;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 15;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 16;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 17;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 18;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 19;
  // a point in time
  string timepoint = 20;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 21;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 22;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 24;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 25;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 26;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 27;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 28;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 29;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 30;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 31;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 32;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 33;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 34;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 35;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 36;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 37;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 38;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 39;
}
// An association between a disease and a phenotypic feature in which the phenotypic feature is associated with the disease in some way.
message DiseaseToPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // a qualifier used in a phenotypic association to state when the phenotype appears is in the subject.
  Onset onset_qualifier = 38;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Disease subject = 39;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  PhenotypicFeature object = 40;
  // number of things with a particular property
  int32 has_count = 41;
  // total number of things in a particular reference set
  int32 has_total = 42;
  double has_quotient = 43;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 44;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 45;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 46;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 47;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 48;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 49;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 50;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 51;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 52;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 53;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 54;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 55;
}
// A substance intended for use in the diagnosis, cure, mitigation, treatment, or prevention of disease
message Drug {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
  string is_supplement = 18;
  // Should be the highest level of FDA approval this chemical entity or device has, regardless of which disease, condition or phenotype it is currently being reviewed to treat.  For specific levels of FDA approval for a specific condition, disease, phenotype, etc., see the association slot, 'clinical approval status.'
  ApprovalStatusEnum highest_FDA_approval_status = 19;
  // An agglomeration of drug regulatory status worldwide. Not specific to FDA.
  ApprovalStatusEnum drug_regulatory_status_world_wide = 20;
  // the method or process of administering a pharmaceutical compound to achieve a therapeutic effect in humans or animals.
  repeated DrugDeliveryEnum routes_of_delivery = 21;
}
// A drug exposure is an intake of a particular drug.
message DrugExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 13;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 14;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 15;
  // a point in time
  string timepoint = 16;
}
// a document accompanying a drug or its container that provides written, printed or graphic information about the drug, including drug contents, specific instructions or warnings for administration, storage and disposal instructions, etc.
message DrugLabel {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // a long-form human readable name for a thing
  string full_name = 7;
  // Alternate human-readable names for a thing
  repeated string synonym = 8;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 9;
  string license = 10;
  string rights = 11;
  string format = 12;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 13;
  // connects an publication to the list of authors who contributed to the publication. This property should be a comma-delimited list of author names. It is recommended that an author's name be formatted as "surname, firstname initial.".   Note that this property is a node annotation expressing the citation list of authorship which might typically otherwise be more completely documented in biolink:PublicationToProviderAssociation defined edges which point to full details about an author and possibly, some qualifiers which clarify the specific status of a given author in the publication.
  repeated Agent authors = 14;
  // When a 2-tuple of page numbers are provided, they represent the start and end page of the publication within its parent publication context. For books, this may be set to the total number of pages of the book.
  repeated string pages = 15;
  // executive  summary of a publication
  string summary = 16;
  // keywords tagging a publication
  repeated string keywords = 17;
  // mesh terms tagging a publication
  repeated string mesh_terms = 18;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 19;
  // Ontology term for publication type may be drawn from Dublin Core types (https://www.dublincore.org/specifications/dublin-core/dcmi-type-vocabulary/), FRBR-aligned Bibliographic Ontology (https://sparontologies.github.io/fabio/current/fabio.html), the MESH publication types (https://www.nlm.nih.gov/mesh/pubtypes.html), the Confederation of Open Access Repositories (COAR) Controlled Vocabulary for Resource Type Genres (http://vocabularies.coar-repositories.org/documentation/resource_types/), Wikidata (https://www.wikidata.org/wiki/Wikidata:Publication_types), or equivalent publication type ontology. When a given publication type ontology term is used within a given knowledge graph, then the CURIE identified term must be documented in the graph as a concept node of biolink:category biolink:OntologyClass.
  repeated string publication_type = 20;
  // Different kinds of publication subtypes will have different preferred identifiers (curies when feasible). Precedence of identifiers for scientific articles is as follows: PMID if available; DOI if not; actual alternate CURIE otherwise. Enclosing publications (i.e. referenced by 'published in' node property) such as books and journals, should have industry-standard identifier such as from ISBN and ISSN.
  string id = 21;
  // the 'title' of the publication is generally recorded in the 'name' property (inherited from NamedThing). The field name 'title' is now also tagged as an acceptable alias for the node property 'name' (just in case).
  string name = 22;
}
// An interaction between a drug and another entity
message DrugToEntityAssociationMixin {
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
// An interaction between a drug and a gene or gene product.
message DrugToGeneAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 9;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 10;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 11;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 12;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 13;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 14;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 15;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 16;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 17;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 18;
  // a point in time
  string timepoint = 19;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 20;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 21;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 24;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 25;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 26;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 27;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 28;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 29;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 30;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 31;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 32;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 33;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 34;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 35;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 36;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 37;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 38;
  // the gene or gene product that is affected by the drug
  GeneOrGeneProduct object = 39;
}
// drug to gene interaction exposure is a drug exposure is where the interactions of the drug with specific genes are known to constitute an 'exposure' to the organism, leading to or influencing an outcome.
message DrugToGeneInteractionExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 13;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 14;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 15;
  // a point in time
  string timepoint = 16;
  // connects an entity with one or more gene or gene products
  repeated Gene has_gene_or_gene_product = 17;
}
message DruggableGeneToDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 11;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 12;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 13;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 14;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 15;
  // a point in time
  string timepoint = 16;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 17;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 18;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 19;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 21;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 22;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 23;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 24;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 25;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 26;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 27;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 28;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 29;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 30;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 31;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 32;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 33;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 34;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 35;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum subject_aspect_qualifier = 36;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 37;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 38;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 39;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 40;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 41;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 42;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 43;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 44;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 45;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 46;
  // number of things with a particular property
  int32 has_count = 47;
  // total number of things in a particular reference set
  int32 has_total = 48;
  double has_quotient = 49;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 50;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Disease object = 51;
  // gene in which variation is correlated with the disease in a protective manner, or if the product produced by the gene can be targeted by a small molecule and this leads to a protective or improving disease state.
  GeneOrGeneProduct subject = 52;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 53;
  // connects an association to an instance of supporting evidence
  repeated DruggableGeneCategoryEnum has_evidence = 54;
}
// Root Biolink Model class for all things and informational relationships, real or imagined.
message Entity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 3;
  repeated string type = 4;
  // A human-readable name for an attribute or entity.
  string name = 5;
  // a human-readable description of an entity
  string description = 6;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 7;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 8;
}
message EntityToDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 9;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 10;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 11;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 12;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 13;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 14;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 15;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 16;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 17;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 18;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 19;
  // a point in time
  string timepoint = 20;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 21;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 22;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 24;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 25;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 26;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 27;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 28;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 29;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 30;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 31;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 32;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 33;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 34;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 35;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 36;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 37;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 38;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 39;
  ClinicalApprovalStatusEnum clinical_approval_status = 40;
  ResearchPhaseEnum max_research_phase = 41;
}
// mixin class for any association whose object (target node) is a disease
message EntityToDiseaseAssociationMixin {
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 1;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 2;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 3;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 4;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 5;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 6;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 7;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 8;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 9;
  // disease
  Disease object = 10;
}
message EntityToDiseaseOrPhenotypicFeatureAssociationMixin {
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // disease or phenotype
  DiseaseOrPhenotypicFeature object = 3;
}
// An association between some entity and an exposure event.
message EntityToExposureEventAssociationMixin {
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  ExposureEvent object = 3;
}
// Qualifiers for entity to disease or phenotype associations.
message EntityToFeatureOrDiseaseQualifiersMixin {
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 1;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 2;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 3;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 4;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 5;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 6;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 7;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 8;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 9;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 10;
}
// An association between some entity and an outcome
message EntityToOutcomeAssociationMixin {
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Outcome object = 3;
}
message EntityToPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 9;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 10;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 11;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 12;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 13;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 14;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 15;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 16;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 17;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 18;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 19;
  // a point in time
  string timepoint = 20;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 21;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 22;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 24;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 25;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 26;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 27;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 28;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 29;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 30;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 31;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 32;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 33;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 34;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 35;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 36;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 37;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 38;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 39;
  ClinicalApprovalStatusEnum clinical_approval_status = 40;
  ResearchPhaseEnum max_research_phase = 41;
}
message EntityToPhenotypicFeatureAssociationMixin {
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 1;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 2;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 3;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 4;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 5;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  PhenotypicFeature object = 9;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 10;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 11;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 12;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 13;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 14;
  // number of things with a particular property
  int32 has_count = 15;
  // total number of things in a particular reference set
  int32 has_total = 16;
  double has_quotient = 17;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 18;
}
// A environmental exposure is a factor relating to abiotic processes in the environment including sunlight (UV-B), atmospheric (heat, cold, general pollution) and water-born contaminants.
message EnvironmentalExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
  // a point in time
  string timepoint = 16;
}
message EnvironmentalFeature {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
message EnvironmentalFoodContaminant {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
}
message EnvironmentalProcess {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// An epidemiological outcome, such as societal disease burden, resulting from an exposure event.
message EpidemiologicalOutcome {
}
message EpigenomicEntity {
  // connects a genomic feature to its sequence
  string has_biological_sequence = 1;
}
// Something that happens at a given place and time.
message Event {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// Class of evidence that supports an association
message EvidenceType {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// A region of the transcript sequence within a gene which is not removed from the primary RNA transcript by RNA splicing.
message Exon {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// A transcript is formed from multiple exons
message ExonToTranscriptRelationship {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Exon subject = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Transcript object = 39;
}
// A (possibly time bounded) incidence of a feature of the environment of an organism that influences one or more phenotypic features of that organism, potentially mediated by genes
message ExposureEvent {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // a point in time
  string timepoint = 2;
}
// An association between an exposure event and an outcome.
message ExposureEventToOutcomeAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 9;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 10;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 11;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 12;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 13;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 14;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 15;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 16;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 17;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 18;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 19;
  // a point in time
  string timepoint = 20;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 21;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 22;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 24;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 25;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 26;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 27;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 28;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 29;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 30;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 31;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 32;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 33;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 34;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 35;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 36;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 37;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 38;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 39;
  // a biological population (general, study, cohort, etc.) with a specific set of characteristics to constrain an association.
  PopulationOfIndividualOrganisms population_context_qualifier = 40;
  // a constraint of time placed upon the truth value of an association. for time intervales, use temporal interval qualifier.
  string temporal_context_qualifier = 41;
}
// Any association between an environment and a phenotypic feature, where being in the environment influences the phenotype.
message ExposureEventToPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 8;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 9;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 10;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 11;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 12;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 13;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 14;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 15;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 16;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 17;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 18;
  // a point in time
  string timepoint = 19;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 20;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 21;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 24;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 25;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 26;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 27;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 28;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 29;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 30;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 31;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 32;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 33;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 34;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 35;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 36;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 37;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 38;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  ExposureEvent subject = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 46;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 47;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 48;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 49;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 50;
  // number of things with a particular property
  int32 has_count = 51;
  // total number of things in a particular reference set
  int32 has_total = 52;
  double has_quotient = 53;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 54;
}
// Qualifiers for disease or phenotype to entity associations.
message FeatureOrDiseaseQualifiersToEntityMixin {
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 1;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 2;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 3;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 4;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 5;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 6;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 7;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 8;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 9;
}
// A substance consumed by a living organism as a source of nutrition
message Food {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
  string is_supplement = 18;
  // Should be the highest level of FDA approval this chemical entity or device has, regardless of which disease, condition or phenotype it is currently being reviewed to treat.  For specific levels of FDA approval for a specific condition, disease, phenotype, etc., see the association slot, 'clinical approval status.'
  ApprovalStatusEnum highest_FDA_approval_status = 19;
  // An agglomeration of drug regulatory status worldwide. Not specific to FDA.
  ApprovalStatusEnum drug_regulatory_status_world_wide = 20;
  // the method or process of administering a pharmaceutical compound to achieve a therapeutic effect in humans or animals.
  repeated DrugDeliveryEnum routes_of_delivery = 21;
}
message FoodAdditive {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
}
// Qualifier for frequency type associations
message FrequencyQualifierMixin {
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 1;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 2;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 3;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 4;
}
message FrequencyQuantifier {
  // number of things with a particular property
  int32 has_count = 1;
  // total number of things in a particular reference set
  int32 has_total = 2;
  double has_quotient = 3;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 4;
}
// An association between a macromolecular machine mixin (gene, gene product or complex of gene products) and either a molecular activity, a biological process or a cellular location in which a function is executed.
message FunctionalAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // gene, product or macromolecular complex that has the function associated with the GO term
  MacromolecularMachineMixin subject = 38;
  // class describing the activity, process or localization of the gene product
  OntologyClass object = 39;
}
// A kingdom of eukaryotic, heterotrophic organisms that live as saprobes or parasites, including mushrooms, yeasts, smuts, molds, etc. They reproduce either sexually or asexually, and have life cycles that range from simple to complex. Filamentous fungi refer to those that grow as multicellular colonies (mushrooms and molds).
message Fungus {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// A region (or regions) that includes all of the sequence elements necessary to encode a functional transcript. A gene locus may include regulatory regions, transcribed regions and/or other functional sequence regions.
message Gene {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // Symbol for a particular thing
  string symbol = 14;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 15;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 16;
}
// Describes an effect that a gene or gene product has on a chemical entity (e.g. an impact of on its abundance, activity, localization, processing, transport, etc.)
message GeneAffectsChemicalAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // A qualifier that composes with a core subject/object concept to define a specific type, variant, alternative version of this concept. The composed concept remains a subtype or instance of the core concept. For example, the qualifier ‘mutation’ combines with the core concept ‘Gene X’ to express the compose concept ‘a mutation of Gene X’.  This qualifier specifies a change in the subject of an association (aka: statement).
  ChemicalOrGeneOrGeneProductFormOrVariantEnum subject_form_or_variant_qualifier = 37;
  // defines a specific part/component of the core concept (used in cases there this specific part has no IRI we can use to directly represent it, e.g. 'ESR1 transcript' q: polyA tail).  This qualifier is for the  subject of an association (or statement).
  GeneOrGeneProductOrChemicalPartQualifierEnum subject_part_qualifier = 38;
  // A qualifier that composes with a core subject/object  concept to describe something that is derived from the core concept.  For example, the qualifier ‘metabolite’ combines with a ‘Chemical X’ core concept to express the composed concept ‘a metabolite of Chemical X’.  This qualifier is for the subject of an association  (or statement).
  string subject_derivative_qualifier = 39;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum subject_aspect_qualifier = 40;
  AnatomicalEntity subject_context_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // A qualifier that composes with a core subject/object concept to define a specific type, variant, alternative version of this concept. The composed concept remains a subtype or instance of the core concept. For example, the qualifier ‘mutation’ combines with the core concept ‘Gene X’ to express the compose concept ‘a mutation of Gene X’.  This qualifier specifies a change in the object of an association (aka: statement).
  ChemicalOrGeneOrGeneProductFormOrVariantEnum object_form_or_variant_qualifier = 43;
  // defines a specific part/component of the core concept (used in cases there this specific part has no IRI we can use to directly represent it, e.g. 'ESR1 transcript' q: polyA tail).  This qualifier is for the  object of an association (or statement).
  GeneOrGeneProductOrChemicalPartQualifierEnum object_part_qualifier = 44;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum object_aspect_qualifier = 45;
  AnatomicalEntity object_context_qualifier = 46;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 47;
  // A qualifier that composes with a core subject/object  concept to describe something that is derived from the core concept.  For example, the qualifier ‘metabolite’ combines with a ‘Chemical X’ core concept to express the composed concept ‘a metabolite of Chemical X’.  This qualifier is for the object of an association  (or statement).
  ChemicalEntityDerivativeEnum object_derivative_qualifier = 48;
  // A statement qualifier representing a type of molecular control mechanism through which an effect of a chemical on a gene or gene product is mediated (e.g. 'agonism', 'inhibition', 'allosteric modulation', 'channel blocker')
  CausalMechanismQualifierEnum causal_mechanism_qualifier = 49;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  AnatomicalEntity anatomical_context_qualifier = 50;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 51;
  // A statement qualifier representing a taxonomic category of species in which a relationship expressed in an association took place.
  OrganismTaxon species_context_qualifier = 52;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  GeneOrGeneProduct subject = 53;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 54;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  ChemicalEntity object = 55;
}
message GeneAsAModelOfDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum subject_aspect_qualifier = 37;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 41;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 42;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 43;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 44;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 45;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 46;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 47;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 48;
  // number of things with a particular property
  int32 has_count = 49;
  // total number of things in a particular reference set
  int32 has_total = 50;
  double has_quotient = 51;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 52;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Disease object = 53;
  // A gene that has a role in modeling the disease. This may be a model organism ortholog of a known disease gene, or it may be a gene whose mutants recapitulate core features of the disease.
  GeneOrGeneProduct subject = 54;
}
// Observed gene expression intensity, context (site, stage) and associated phenotypic status within which the expression occurs.
message GeneExpressionMixin {
  // Optional quantitative value indicating degree of expression.
  OntologyClass quantifier_qualifier = 1;
  // location in which gene or protein expression takes place. May be cell, tissue, or organ.
  AnatomicalEntity expression_site = 2;
  // stage during which gene or protein expression of takes place.
  LifeStage stage_qualifier = 3;
  // in experiments (e.g. gene expression) assaying diseased or unhealthy tissue, the phenotypic state can be put here, e.g. MONDO ID. For healthy tissues, use XXX.
  DiseaseOrPhenotypicFeature phenotypic_state = 4;
}
// any grouping of multiple genes or gene products related by common descent
message GeneFamily {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // connects an entity with one or more gene or gene products
  repeated Gene has_gene_or_gene_product = 15;
}
// any grouping of multiple genes or gene products
message GeneGroupingMixin {
  // connects an entity with one or more gene or gene products
  repeated Gene has_gene_or_gene_product = 1;
}
message GeneHasVariantThatContributesToDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum subject_aspect_qualifier = 37;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 38;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 39;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 41;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 42;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 43;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 44;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 45;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 46;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 47;
  // number of things with a particular property
  int32 has_count = 48;
  // total number of things in a particular reference set
  int32 has_total = 49;
  double has_quotient = 50;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 51;
  // A qualifier that composes with a core subject/object concept to define a specific type, variant, alternative version of this concept. The composed concept remains a subtype or instance of the core concept. For example, the qualifier ‘mutation’ combines with the core concept ‘Gene X’ to express the compose concept ‘a mutation of Gene X’.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_form_or_variant_qualifier = 52;
  // A gene that has a role in modeling the disease. This may be a model organism ortholog of a known disease gene, or it may be a gene whose mutants recapitulate core features of the disease.
  GeneOrGeneProduct subject = 53;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Disease object = 54;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 55;
}
// A union of gene loci or gene products. Frequently an identifier for one will be used as proxy for another
message GeneOrGeneProduct {
  // genes are typically designated by a short symbol and a full name. We map the symbol to the default display name and use an additional slot for full name
  string name = 1;
}
// This is an abstract class that can be mixed in with different kinds of gene products to indicate that the gene product is intended to represent a specific isoform rather than a canonical or reference or generic product. The designation of canonical or reference may be arbitrary, or it may represent the superclass of all isoforms.
message GeneProductIsoformMixin {
  // genes are typically designated by a short symbol and a full name. We map the symbol to the default display name and use an additional slot for full name
  string name = 1;
  // Alternate human-readable names for a thing
  repeated string synonym = 2;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 3;
}
// The functional molecular product of a single gene locus. Gene products are either proteins or functional RNA molecules.
message GeneProductMixin {
  // genes are typically designated by a short symbol and a full name. We map the symbol to the default display name and use an additional slot for full name
  string name = 1;
  // Alternate human-readable names for a thing
  repeated string synonym = 2;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 3;
}
// Describes a regulatory relationship between two genes or gene products.
message GeneRegulatesGeneAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the aspect of the object gene or gene product that is being regulated, must be a descendant of  "activity_or_abundance""
  GeneOrGeneProductOrChemicalEntityAspectEnum object_aspect_qualifier = 37;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 38;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 39;
  // A statement qualifier representing a taxonomic category of species in which a relationship expressed in an association took place.
  OrganismTaxon species_context_qualifier = 40;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  GeneOrGeneProduct subject = 41;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 42;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  GeneOrGeneProduct object = 43;
}
message GeneToDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum subject_aspect_qualifier = 37;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 41;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 42;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 43;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 44;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 45;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 46;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 47;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 48;
  // number of things with a particular property
  int32 has_count = 49;
  // total number of things in a particular reference set
  int32 has_total = 50;
  double has_quotient = 51;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 52;
  // gene in which variation is correlated with the disease, may be protective or causative or associative, or as a model
  GeneOrGeneProduct subject = 53;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Disease object = 54;
}
message GeneToDiseaseOrPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum subject_aspect_qualifier = 37;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 38;
  // gene in which variation is correlated with the phenotypic feature
  GeneOrGeneProduct subject = 39;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  DiseaseOrPhenotypicFeature object = 40;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 41;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 42;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 43;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 46;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 47;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 48;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 49;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 50;
  // number of things with a particular property
  int32 has_count = 51;
  // total number of things in a particular reference set
  int32 has_total = 52;
  double has_quotient = 53;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 54;
}
message GeneToEntityAssociationMixin {
  // gene that is the subject of the association
  GeneOrGeneProduct subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
// An association between a gene and a gene expression site, possibly qualified by stage/timing info.
message GeneToExpressionSiteAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // stage at which the gene is expressed in the site
  LifeStage stage_qualifier = 37;
  // can be used to indicate magnitude, or also ranking
  OntologyClass quantifier_qualifier = 38;
  // Gene or gene product positively within the specified anatomical entity (or subclass, i.e. cellular component) location.
  GeneOrGeneProduct subject = 39;
  // location in which the gene is expressed
  AnatomicalEntity object = 40;
  // expression relationship
  string predicate = 41;
}
// abstract parent class for different kinds of gene-gene or gene product to gene product relationships. Includes homology and interaction.
message GeneToGeneAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // the subject gene in the association. If the relation is symmetric, subject vs object is arbitrary. We allow a gene product to stand as a proxy for the gene or vice versa.
  GeneOrGeneProduct subject = 38;
  // the object gene in the association. If the relation is symmetric, subject vs object is arbitrary. We allow a gene product to stand as a proxy for the gene or vice versa.
  GeneOrGeneProduct object = 39;
}
// Indicates that two genes are co-expressed, generally under the same conditions.
message GeneToGeneCoexpressionAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the subject gene in the association. If the relation is symmetric, subject vs object is arbitrary. We allow a gene product to stand as a proxy for the gene or vice versa.
  GeneOrGeneProduct subject = 37;
  // the object gene in the association. If the relation is symmetric, subject vs object is arbitrary. We allow a gene product to stand as a proxy for the gene or vice versa.
  GeneOrGeneProduct object = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
  // Optional quantitative value indicating degree of expression.
  OntologyClass quantifier_qualifier = 40;
  // location in which gene or protein expression takes place. May be cell, tissue, or organ.
  AnatomicalEntity expression_site = 41;
  // stage during which gene or protein expression of takes place.
  LifeStage stage_qualifier = 42;
  // in experiments (e.g. gene expression) assaying diseased or unhealthy tissue, the phenotypic state can be put here, e.g. MONDO ID. For healthy tissues, use XXX.
  DiseaseOrPhenotypicFeature phenotypic_state = 43;
}
// Set membership of a gene in a family of genes related by common evolutionary ancestry usually inferred by sequence comparisons. The genes in a given family generally share common sequence motifs which generally map onto shared gene product structure-function relationships.
message GeneToGeneFamilyAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Gene subject = 37;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  GeneFamily object = 38;
  // membership of the gene in the given gene family.
  string predicate = 39;
}
// A homology association between two genes. May be orthology (in which case the species of subject and object should differ) or paralogy (in which case the species may be the same)
message GeneToGeneHomologyAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  GeneOrGeneProduct subject = 37;
  // homology relationship type
  string predicate = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  GeneOrGeneProduct object = 39;
}
// A gene is transcribed and potentially translated to a gene product
message GeneToGeneProductRelationship {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Gene subject = 37;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  GeneProductMixin object = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
}
message GeneToGoTermAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // gene, product or macromolecular complex that has the function associated with the GO term
  Gene subject = 38;
  // class describing the activity, process or localization of the gene product
  OntologyClass object = 39;
}
// An interaction between a gene or gene product and a biological process or pathway.
message GeneToPathwayAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // the gene or gene product entity that participates or influences the pathway
  GeneOrGeneProduct subject = 38;
  // the pathway that includes or is affected by the gene or gene product
  Pathway object = 39;
}
message GeneToPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  GeneOrGeneProductOrChemicalEntityAspectEnum subject_aspect_qualifier = 37;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 41;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 42;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 43;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 44;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 45;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 46;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 47;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 48;
  // number of things with a particular property
  int32 has_count = 49;
  // total number of things in a particular reference set
  int32 has_total = 50;
  double has_quotient = 51;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 52;
  // gene in which variation is correlated with the phenotypic feature
  GeneOrGeneProduct subject = 53;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  PhenotypicFeature object = 54;
}
// The pattern or 'mode' in which a particular genetic trait or disorder is passed from one generation to the next, e.g. autosomal dominant, autosomal recessive, etc.
message GeneticInheritance {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// A genome is the sum of genetic material within a cell or virion.
message Genome {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 15;
}
// A genomic background exposure is where an individual's specific genomic background of genes, sequence variants or other pre-existing genomic conditions constitute a kind of 'exposure' to the organism, leading to or influencing an outcome.
message GenomicBackgroundExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
  // a point in time
  string timepoint = 16;
  // connects an entity with one or more gene or gene products
  repeated Gene has_gene_or_gene_product = 17;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 18;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 19;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 20;
}
message GenomicEntity {
  // connects a genomic feature to its sequence
  string has_biological_sequence = 1;
}
// A relationship between a sequence feature and a nucleic acid entity it is localized to. The reference entity may be a chromosome, chromosome region or information entity such as a contig.
message GenomicSequenceLocalization {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // The position at which the subject nucleic acid entity starts on the chromosome or other entity to which it is located on. (ie: the start of the sequence being referenced is 0).
  int32 start_interbase_coordinate = 37;
  // The position at which the subject nucleic acid entity ends on the chromosome or other entity to which it is located on.
  int32 end_interbase_coordinate = 38;
  // The version of the genome on which a feature is located. For example, GRCh38 for Homo sapiens.
  StrandEnum genome_build = 39;
  // The strand on which a feature is located. Has a value of '+' (sense strand or forward strand) or '-' (anti-sense strand or reverse strand).
  StrandEnum strand = 40;
  // The phase for a coding sequence entity. For example, phase of a CDS as represented in a GFF3 with a value of 0, 1 or 2.
  PhaseEnum phase = 41;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NucleicAcidEntity subject = 42;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NucleicAcidEntity object = 43;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 44;
}
// An information content entity that describes a genome by specifying the total variation in genomic sequence and/or gene expression, relative to some established background
message Genotype {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  Zygosity has_zygosity = 15;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 16;
}
message GenotypeAsAModelOfDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // E.g. is pathogenic for
  string predicate = 37;
  // a disease that is associated with that genotype
  NamedThing object = 38;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 39;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 40;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 41;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 42;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 43;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 44;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 45;
  // A genotype that has a role in modeling the disease.
  Genotype subject = 46;
}
message GenotypeToDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // a genotype that is associated in some way with a disease state
  NamedThing subject = 37;
  // E.g. is pathogenic for
  string predicate = 38;
  // a disease that is associated with that genotype
  NamedThing object = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 46;
}
message GenotypeToEntityAssociationMixin {
  // genotype that is the subject of the association
  Genotype subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
// Any association between a genotype and a gene. The genotype have have multiple variants in that gene or a single one. There is no assumption of cardinality
message GenotypeToGeneAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the relationship type used to connect genotype to gene
  string predicate = 37;
  // parent genotype
  Genotype subject = 38;
  // gene implicated in genotype
  Gene object = 39;
}
// Any association between one genotype and a genotypic entity that is a sub-component of it
message GenotypeToGenotypePartAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 37;
  // parent genotype
  Genotype subject = 38;
  // child genotype
  Genotype object = 39;
}
// Any association between one genotype and a phenotypic feature, where having the genotype confers the phenotype, either in isolation or through environment
message GenotypeToPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 38;
  // genotype that is associated with the phenotypic feature
  Genotype subject = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 46;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 47;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 48;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 49;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 50;
  // number of things with a particular property
  int32 has_count = 51;
  // total number of things in a particular reference set
  int32 has_total = 52;
  double has_quotient = 53;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 54;
}
// Any association between a genotype and a sequence variant.
message GenotypeToVariantAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the relationship type used to connect genotype to gene
  string predicate = 37;
  // parent genotype
  Genotype subject = 38;
  // gene implicated in genotype
  SequenceVariant object = 39;
}
// An attribute corresponding to the genotypic sex of the individual, based upon genotypic composition of sex chromosomes.
message GenotypicSex {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// A geographic exposure is a factor relating to geographic proximity to some impactful entity.
message GeographicExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
  // a point in time
  string timepoint = 16;
}
// a location that can be described in lat/long coordinates
message GeographicLocation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // latitude
  float latitude = 13;
  // longitude
  float longitude = 14;
}
// a location that can be described in lat/long coordinates, for a particular time
message GeographicLocationAtTime {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // latitude
  float latitude = 13;
  // longitude
  float longitude = 14;
  // a point in time
  string timepoint = 15;
}
message GrossAnatomicalStructure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// A set of zero or more Alleles on a single instance of a Sequence[VMC]
message Haplotype {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 15;
}
message Hospitalization {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// An outcome resulting from an exposure event which is the increased manifestation of acute (e.g. emergency room visit) or chronic (inpatient) hospitalization.
message HospitalizationOutcome {
}
// A member of the the species Homo sapiens.
message Human {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// An instance of an organism. For example, Richard Nixon, Charles Darwin, my pet cat. Example ID: ORCID:0000-0002-5355-2576
message IndividualOrganism {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// a piece of information that typically describes some topic of discourse or is used as support.
message InformationContentEntity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// association between a named thing and a information content entity where the specific context of the relationship between that named thing and the publication is unknown. For example, model organisms databases often capture the knowledge that a gene is found in a journal article, but not specifically the context in which that gene was documented in the article. In these cases, this association with the accompanying predicate 'mentions' could be used. Conversely, for more specific associations (like 'gene to disease association', the publication should be captured as an edge property).
message InformationContentEntityToNamedThingAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 37;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
}
// An animal lacking a vertebral column. This group consists of 98% of all animal species.
message Invertebrate {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// an article, typically presenting results of research, that is published in an issue of a scientific journal.
message JournalArticle {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // a long-form human readable name for a thing
  string full_name = 7;
  // Alternate human-readable names for a thing
  repeated string synonym = 8;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 9;
  string license = 10;
  string rights = 11;
  string format = 12;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 13;
  // connects an publication to the list of authors who contributed to the publication. This property should be a comma-delimited list of author names. It is recommended that an author's name be formatted as "surname, firstname initial.".   Note that this property is a node annotation expressing the citation list of authorship which might typically otherwise be more completely documented in biolink:PublicationToProviderAssociation defined edges which point to full details about an author and possibly, some qualifiers which clarify the specific status of a given author in the publication.
  repeated Agent authors = 14;
  // When a 2-tuple of page numbers are provided, they represent the start and end page of the publication within its parent publication context. For books, this may be set to the total number of pages of the book.
  repeated string pages = 15;
  // executive  summary of a publication
  string summary = 16;
  // keywords tagging a publication
  repeated string keywords = 17;
  // mesh terms tagging a publication
  repeated string mesh_terms = 18;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 19;
  // Ontology term for publication type may be drawn from Dublin Core types (https://www.dublincore.org/specifications/dublin-core/dcmi-type-vocabulary/), FRBR-aligned Bibliographic Ontology (https://sparontologies.github.io/fabio/current/fabio.html), the MESH publication types (https://www.nlm.nih.gov/mesh/pubtypes.html), the Confederation of Open Access Repositories (COAR) Controlled Vocabulary for Resource Type Genres (http://vocabularies.coar-repositories.org/documentation/resource_types/), Wikidata (https://www.wikidata.org/wiki/Wikidata:Publication_types), or equivalent publication type ontology. When a given publication type ontology term is used within a given knowledge graph, then the CURIE identified term must be documented in the graph as a concept node of biolink:category biolink:OntologyClass.
  repeated string publication_type = 20;
  // Different kinds of publication subtypes will have different preferred identifiers (curies when feasible). Precedence of identifiers for scientific articles is as follows: PMID if available; DOI if not; actual alternate CURIE otherwise. Enclosing publications (i.e. referenced by 'published in' node property) such as books and journals, should have industry-standard identifier such as from ISBN and ISSN.
  string id = 21;
  // the 'title' of the publication is generally recorded in the 'name' property (inherited from NamedThing). The field name 'title' is now also tagged as an acceptable alias for the node property 'name' (just in case).
  string name = 22;
  // The enclosing parent serial containing the article should have industry-standard identifier from ISSN.
  string published_in = 23;
  // Optional value, if used locally as a convenience, is set to the iso abbreviation of the 'published in' parent.
  string iso_abbreviation = 24;
  // volume of a book or music release in a collection/series or a published collection of journal issues in a serial publication
  string volume = 25;
  // issue of a newspaper, a scientific journal or magazine for reference purpose
  string issue = 26;
}
// A stage of development or growth of an organism, including post-natal adult stages
message LifeStage {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// A result of a log odds ratio analysis.
message LogOddsAnalysisResult {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// A stable assembly of two or more macromolecules, i.e. proteins, nucleic acids, carbohydrates or lipids, in which at least one component is a protein and the constituent parts function together.
message MacromolecularComplex {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// A union of gene locus, gene product, and macromolecular complex. These are the basic units of function in a cell. They either carry out individual biological activities, or they encode molecules which do this.
message MacromolecularMachineMixin {
  // genes are typically designated by a short symbol and a full name. We map the symbol to the default display name and use an additional slot for full name
  string name = 1;
}
// A functional association between a macromolecular machine (gene, gene product or complex) and a biological process or pathway (as represented in the GO biological process branch), where the entity carries out some part of the process, regulates it, or acts upstream of it.
message MacromolecularMachineToBiologicalProcessAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // gene, product or macromolecular complex that has the function associated with the GO term
  MacromolecularMachineMixin subject = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  BiologicalProcess object = 39;
  // A statement qualifier representing a taxonomic category of species in which a relationship expressed in an association took place.
  OrganismTaxon species_context_qualifier = 40;
}
// A functional association between a macromolecular machine (gene, gene product or complex) and a cellular component (as represented in the GO cellular component branch), where the entity carries out its function in the cellular component.
message MacromolecularMachineToCellularComponentAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // gene, product or macromolecular complex that has the function associated with the GO term
  MacromolecularMachineMixin subject = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  CellularComponent object = 39;
  // A statement qualifier representing a taxonomic category of species in which a relationship expressed in an association took place.
  OrganismTaxon species_context_qualifier = 40;
}
// an association which has a macromolecular machine mixin as a subject
message MacromolecularMachineToEntityAssociationMixin {
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
  // A statement qualifier representing a taxonomic category of species in which a relationship expressed in an association took place.
  OrganismTaxon species_context_qualifier = 4;
}
// A functional association between a macromolecular machine (gene, gene product or complex) and a molecular activity (as represented in the GO molecular function branch), where the entity carries out the activity, or contributes to its execution.
message MacromolecularMachineToMolecularActivityAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // gene, product or macromolecular complex that has the function associated with the GO term
  MacromolecularMachineMixin subject = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  MolecularActivity object = 39;
  // A statement qualifier representing a taxonomic category of species in which a relationship expressed in an association took place.
  OrganismTaxon species_context_qualifier = 40;
}
// A member of the class Mammalia, a clade of endothermic amniotes distinguished from reptiles and birds by the possession of hair, three middle ear bones, mammary glands, and a neocortex
message Mammal {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// A collection of deprecated mappings.
message MappingCollection {
  // A collection of relationships that are not used in biolink, but have biolink patterns that can be used to replace them.  This is a temporary slot to help with the transition to the fully qualified predicate model in Biolink3.
  repeated PredicateMapping predicate_mappings = 1;
}
// A sample is a limited quantity of something (e.g. an individual or set of individuals from a population, or a portion of a substance) to be used for testing, analysis, inspection, investigation, demonstration, or trial use. [SIO]
message MaterialSample {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// An association between a material sample and the material entity from which it is derived.
message MaterialSampleDerivationAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the material sample being described
  MaterialSample subject = 37;
  // derivation relationship
  string predicate = 38;
  // the material entity the sample was derived from. This may be another material sample, or any other material entity, including for example an organism, a geographic feature, or some environmental material.
  NamedThing object = 39;
}
// An association between a material sample and a disease or phenotype.
message MaterialSampleToDiseaseOrPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 9;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 10;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 11;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 12;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 13;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 14;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 15;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 16;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 17;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 18;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 19;
  // a point in time
  string timepoint = 20;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 21;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 22;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 24;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 25;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 26;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 27;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 28;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 29;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 30;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 31;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 32;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 33;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 34;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 35;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 36;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 37;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 38;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 39;
}
// An association between a material sample and something.
message MaterialSampleToEntityAssociationMixin {
  // the material sample being described
  MaterialSample subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
message MicroRNA {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// This mixin is used for any association class for which the subject (source node) plays the role of a 'model', in that it recapitulates some features of the disease in a way that is useful for studying the disease outside a patient carrying the disease
message ModelToDiseaseAssociationMixin {
  // The entity that serves as the model of the disease. This may be an organism, a strain of organism, a genotype or variant that exhibits similar features, or a gene that when mutated exhibits features of the disease
  NamedThing subject = 1;
  // The relationship to the disease
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
// An execution of a molecular function carried out by a gene product or macromolecular complex.
message MolecularActivity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // A chemical entity that is the input for the reaction
  repeated MolecularEntity has_input = 15;
  // A chemical entity that is the output for the reaction
  repeated MolecularEntity has_output = 16;
  // The gene product, gene, or complex that catalyzes the reaction
  repeated MacromolecularMachineMixin enabled_by = 17;
}
// Added in response to capturing relationship between microbiome activities as measured via measurements of blood analytes as collected via blood and stool samples
message MolecularActivityToChemicalEntityAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  MolecularActivity subject = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  ChemicalEntity object = 39;
}
// Added in response to capturing relationship between microbiome activities as measured via measurements of blood analytes as collected via blood and stool samples
message MolecularActivityToMolecularActivityAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  MolecularActivity subject = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  MolecularActivity object = 39;
}
// Association that holds the relationship between a reaction and the pathway it participates in.
message MolecularActivityToPathwayAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  MolecularActivity subject = 37;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Pathway object = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
}
// A molecular entity is a chemical entity composed of individual or covalently bonded atoms.
message MolecularEntity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
  // indicates whether a molecular entity is a metabolite
  bool is_metabolite = 18;
}
// A molecular mixture is a chemical mixture composed of two or more molecular entities with known concentration and stoichiometry.
message MolecularMixture {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
  string is_supplement = 18;
  // Should be the highest level of FDA approval this chemical entity or device has, regardless of which disease, condition or phenotype it is currently being reviewed to treat.  For specific levels of FDA approval for a specific condition, disease, phenotype, etc., see the association slot, 'clinical approval status.'
  ApprovalStatusEnum highest_FDA_approval_status = 19;
  // An agglomeration of drug regulatory status worldwide. Not specific to FDA.
  ApprovalStatusEnum drug_regulatory_status_world_wide = 20;
  // the method or process of administering a pharmaceutical compound to achieve a therapeutic effect in humans or animals.
  repeated DrugDeliveryEnum routes_of_delivery = 21;
}
// An outcome of death from resulting from an exposure event.
message MortalityOutcome {
}
// a databased entity or concept/class
message NamedThing {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
message NamedThingAssociatedWithLikelihoodOfNamedThingAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 37;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 38;
  OntologyClass subject_context_qualifier = 39;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 40;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 41;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 42;
  OntologyClass object_context_qualifier = 43;
  // a biological population (general, study, cohort, etc.) with a specific set of characteristics to constrain an association.
  PopulationOfIndividualOrganisms population_context_qualifier = 44;
}
message NoncodingRNAProduct {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// A nucleic acid entity is a molecular entity characterized by availability in gene databases of nucleotide-based sequence representations of its precise sequence; for convenience of representation, partial sequences of various kinds are included.
message NucleicAcidEntity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
  // indicates whether a molecular entity is a metabolite
  bool is_metabolite = 18;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 19;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 20;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 21;
}
// A linear nucleotide sequence pattern that is widespread and has, or is conjectured to have, a biological significance. e.g. the TATA box promoter motif, transcription factor binding consensus sequences.
message NucleicAcidSequenceMotif {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// A chemical modification of a histone protein within a nucleosome octomer or a substitution of a histone with a variant histone isoform. e.g. Histone 4 Lysine 20 methylation (H4K20me), histone variant H2AZ substituting H2A.
message NucleosomeModification {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 15;
}
// A result of a observed expected frequency analysis.
message ObservedExpectedFrequencyAnalysisResult {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// A processual entity.
message Occurrent {
}
// The age group in which (disease) symptom manifestations appear.
message Onset {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// a concept or class in an ontology, vocabulary or thesaurus. Note that nodes in a biolink compatible KG can be considered both instances of biolink classes, and OWL classes in their own right. In general you should not need to use this class directly. Instead, use the appropriate biolink class. For example, for the GO concept of endocytosis (GO:0006897), use bl:BiologicalProcess as the type.
message OntologyClass {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
}
// describes a characteristic of an organismal entity.
message OrganismAttribute {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// A classification of a set of organisms. Example instances: NCBITaxon:9606 (Homo sapiens), NCBITaxon:2 (Bacteria). Can also be used to represent strains or subspecies.
message OrganismTaxon {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  TaxonomicRank has_taxonomic_rank = 13;
}
// An association between an organism taxon and another entity
message OrganismTaxonToEntityAssociation {
  // organism taxon that is the subject of the association
  OrganismTaxon subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
message OrganismTaxonToEnvironmentAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the taxon that is the subject of the association
  OrganismTaxon subject = 37;
  // the environment in which the organism occurs
  NamedThing object = 38;
  // predicate describing the relationship between the taxon and the environment
  string predicate = 39;
}
// A relationship between two organism taxon nodes
message OrganismTaxonToOrganismTaxonAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  OrganismTaxon subject = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  OrganismTaxon object = 39;
}
// An interaction relationship between two taxa. This may be a symbiotic relationship (encompassing mutualism and parasitism), or it may be non-symbiotic. Example: plague transmitted_by flea; cattle domesticated_by Homo sapiens; plague infects Homo sapiens
message OrganismTaxonToOrganismTaxonInteraction {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the environment in which the two taxa interact
  string associated_environmental_context = 37;
  // the taxon that is the subject of the association
  OrganismTaxon subject = 38;
  // the taxon that is the subject of the association
  OrganismTaxon object = 39;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 40;
}
// A child-parent relationship between two taxa. For example: Homo sapiens subclass_of Homo
message OrganismTaxonToOrganismTaxonSpecialization {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the more specific taxon
  OrganismTaxon subject = 37;
  // the more general taxon
  OrganismTaxon object = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
}
message OrganismToOrganismAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  IndividualOrganism subject = 38;
  // An association between two individual organisms.
  IndividualOrganism object = 39;
}
// A named entity that is either a part of an organism, a whole organism, population or clade of organisms, excluding chemical entities
message OrganismalEntity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
message OrganismalEntityAsAModelOfDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 8;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 9;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 10;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 11;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 12;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 13;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 14;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 15;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 16;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 17;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 18;
  // a point in time
  string timepoint = 19;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 20;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 21;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 24;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 25;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 26;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 27;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 28;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 29;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 30;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 31;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 32;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 33;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 34;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 35;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 36;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 37;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 38;
  // A organismal entity (strain, breed) with a predisposition to a disease, or bred/created specifically to model a disease.
  OrganismalEntity subject = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 46;
}
// An entity that has the role of being the consequence of an exposure event. This is an abstract mixin grouping of various categories of possible biological or non-biological (e.g. clinical) outcomes.
message Outcome {
}
// An interaction between two genes or two gene products. May be physical (e.g. protein binding) or genetic (between genes). May be symmetric (e.g. protein interaction) or directed (e.g. phosphorylation)
message PairwiseGeneToGeneInteraction {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the subject gene in the association. If the relation is symmetric, subject vs object is arbitrary. We allow a gene product to stand as a proxy for the gene or vice versa.
  GeneOrGeneProduct subject = 37;
  // the object gene in the association. If the relation is symmetric, subject vs object is arbitrary. We allow a gene product to stand as a proxy for the gene or vice versa.
  GeneOrGeneProduct object = 38;
  // interaction relationship type
  string predicate = 39;
}
// An interaction at the molecular level between two physical entities
message PairwiseMolecularInteraction {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  // A human-readable name for an attribute or entity.
  string name = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 6;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 7;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 8;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 9;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 10;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 11;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 12;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 13;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 14;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 15;
  // a point in time
  string timepoint = 16;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 17;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 18;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 19;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 21;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 22;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 23;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 24;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 25;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 26;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 27;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 28;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 29;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 30;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 31;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 32;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 33;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 34;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 35;
  OntologyClass interacting_molecules_category = 36;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  MolecularEntity subject = 37;
  // identifier for the interaction. This may come from an interaction database such as IMEX.
  string id = 38;
  // interaction relationship type
  string predicate = 39;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  MolecularEntity object = 40;
}
// a legal document granted by a patent issuing authority which confers upon the patenter the sole right to make, use and sell an invention for a set period of time.
message Patent {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // a long-form human readable name for a thing
  string full_name = 7;
  // Alternate human-readable names for a thing
  repeated string synonym = 8;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 9;
  string license = 10;
  string rights = 11;
  string format = 12;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 13;
  // connects an publication to the list of authors who contributed to the publication. This property should be a comma-delimited list of author names. It is recommended that an author's name be formatted as "surname, firstname initial.".   Note that this property is a node annotation expressing the citation list of authorship which might typically otherwise be more completely documented in biolink:PublicationToProviderAssociation defined edges which point to full details about an author and possibly, some qualifiers which clarify the specific status of a given author in the publication.
  repeated Agent authors = 14;
  // When a 2-tuple of page numbers are provided, they represent the start and end page of the publication within its parent publication context. For books, this may be set to the total number of pages of the book.
  repeated string pages = 15;
  // executive  summary of a publication
  string summary = 16;
  // keywords tagging a publication
  repeated string keywords = 17;
  // mesh terms tagging a publication
  repeated string mesh_terms = 18;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 19;
  // Ontology term for publication type may be drawn from Dublin Core types (https://www.dublincore.org/specifications/dublin-core/dcmi-type-vocabulary/), FRBR-aligned Bibliographic Ontology (https://sparontologies.github.io/fabio/current/fabio.html), the MESH publication types (https://www.nlm.nih.gov/mesh/pubtypes.html), the Confederation of Open Access Repositories (COAR) Controlled Vocabulary for Resource Type Genres (http://vocabularies.coar-repositories.org/documentation/resource_types/), Wikidata (https://www.wikidata.org/wiki/Wikidata:Publication_types), or equivalent publication type ontology. When a given publication type ontology term is used within a given knowledge graph, then the CURIE identified term must be documented in the graph as a concept node of biolink:category biolink:OntologyClass.
  repeated string publication_type = 20;
  // Different kinds of publication subtypes will have different preferred identifiers (curies when feasible). Precedence of identifiers for scientific articles is as follows: PMID if available; DOI if not; actual alternate CURIE otherwise. Enclosing publications (i.e. referenced by 'published in' node property) such as books and journals, should have industry-standard identifier such as from ISBN and ISSN.
  string id = 21;
  // the 'title' of the publication is generally recorded in the 'name' property (inherited from NamedThing). The field name 'title' is now also tagged as an acceptable alias for the node property 'name' (just in case).
  string name = 22;
}
// A relationship quantifier between a variant or symptom and a disease, which is high when the presence of the feature implies the existence of the disease
message PathognomonicityQuantifier {
}
// An abnormal anatomical structure, when viewed as an exposure, representing an precondition, leading to or influencing an outcome, e.g. thrombosis leading to an ischemic disease outcome.
message PathologicalAnatomicalExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
  // a point in time
  string timepoint = 16;
}
// An outcome resulting from an exposure event which is the manifestation of an abnormal anatomical structure.
message PathologicalAnatomicalOutcome {
}
// An anatomical structure with the potential of have an abnormal or deleterious effect at the subcellular, cellular, multicellular, or organismal level.
message PathologicalAnatomicalStructure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// A pathological (abnormal) structure or process.
message PathologicalEntityMixin {
}
// A biologic function or a process having an abnormal or deleterious effect at the subcellular, cellular, multicellular, or organismal level.
message PathologicalProcess {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // holds between a process and a continuant, where the continuant is an input into the process
  repeated NamedThing has_input = 15;
  // holds between a process and a continuant, where the continuant is an output of the process
  repeated NamedThing has_output = 16;
  // holds between a process and a physical entity, where the physical entity executes the process
  repeated PhysicalEntity enabled_by = 17;
}
// A pathological process, when viewed as an exposure, representing a precondition, leading to or influencing an outcome, e.g. autoimmunity leading to disease.
message PathologicalProcessExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
  // a point in time
  string timepoint = 16;
}
// An outcome resulting from an exposure event which is the manifestation of a pathological process.
message PathologicalProcessOutcome {
}
message Pathway {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // holds between a process and a continuant, where the continuant is an input into the process
  repeated NamedThing has_input = 15;
  // holds between a process and a continuant, where the continuant is an output of the process
  repeated NamedThing has_output = 16;
  // holds between a process and a physical entity, where the physical entity executes the process
  repeated PhysicalEntity enabled_by = 17;
}
// a fact or situation that is observed to exist or happen, especially one whose cause or explanation is in question
message Phenomenon {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// A combination of entity and quality that makes up a phenotyping statement. An observable characteristic of an individual resulting from the interaction of its genotype with its molecular and physical environment.
message PhenotypicFeature {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
message PhenotypicFeatureToDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 8;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 9;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 10;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 11;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 12;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 13;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 14;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 15;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 16;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 17;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 18;
  // a point in time
  string timepoint = 19;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 20;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 21;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 24;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 25;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 26;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 27;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 28;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 29;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 30;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 31;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 32;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 33;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 34;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 35;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 36;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 37;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 46;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 47;
  // number of things with a particular property
  int32 has_count = 48;
  // total number of things in a particular reference set
  int32 has_total = 49;
  double has_quotient = 50;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 51;
}
message PhenotypicFeatureToEntityAssociationMixin {
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 4;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 5;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 6;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 7;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 8;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 9;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  PhenotypicFeature subject = 10;
  // number of things with a particular property
  int32 has_count = 11;
  // total number of things in a particular reference set
  int32 has_total = 12;
  double has_quotient = 13;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 14;
}
// Association between two concept nodes of phenotypic character, qualified by the predicate used. This association may typically be used to specify 'similar_to' or 'member_of' relationships.
message PhenotypicFeatureToPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 9;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 10;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 11;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 12;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 13;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 14;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 15;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 16;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 17;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 18;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 19;
  // a point in time
  string timepoint = 20;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 21;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 22;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 24;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 25;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 26;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 27;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 28;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 29;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 30;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 31;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 32;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 33;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 34;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 35;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 36;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 37;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 38;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 46;
  // number of things with a particular property
  int32 has_count = 47;
  // total number of things in a particular reference set
  int32 has_total = 48;
  double has_quotient = 49;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 50;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 51;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 52;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 53;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 54;
}
// A property of a phenotype
message PhenotypicQuality {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// An attribute corresponding to the phenotypic sex of the individual, based upon the reproductive organs present.
message PhenotypicSex {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// An entity that has material reality (a.k.a. physical essence).
message PhysicalEntity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// Semantic mixin concept.  Pertains to entities that have physical properties such as mass, volume, or charge.
message PhysicalEssence {
}
// Either a physical or processual entity.
message PhysicalEssenceOrOccurrent {
}
message PhysiologicalProcess {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // holds between a process and a continuant, where the continuant is an input into the process
  repeated NamedThing has_input = 15;
  // holds between a process and a continuant, where the continuant is an output of the process
  repeated NamedThing has_output = 16;
  // holds between a process and a physical entity, where the physical entity executes the process
  repeated PhysicalEntity enabled_by = 17;
}
// Any entity or process that exists at the level of the whole planet
message PlanetaryEntity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
message Plant {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// A polypeptide is a molecular entity characterized by availability in protein databases of amino-acid-based sequence representations of its precise primary structure; for convenience of representation, partial sequences of various kinds are included, even if they do not represent a physical molecule.
message Polypeptide {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// A collection of individuals from the same taxonomic class distinguished by one or more characteristics.  Characteristics can include, but are not limited to, shared geographic location, genetics, phenotypes.
message PopulationOfIndividualOrganisms {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// An association between a two populations
message PopulationToPopulationAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // the population that form the subject of the association
  PopulationOfIndividualOrganisms subject = 37;
  // the population that form the object of the association
  PopulationOfIndividualOrganisms object = 38;
  // A relationship type that holds between the subject and object populations. Standard mereological relations can be used. E.g. subject part-of object, subject overlaps object. Derivation relationships can also be used
  string predicate = 39;
}
// A chemical modification of a polypeptide or protein that occurs after translation. e.g. polypeptide cleavage to form separate proteins, methylation or acetylation of histone tail amino acids, protein ubiquitination.
message PosttranslationalModification {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// A deprecated predicate mapping object contains the deprecated predicate and an example of the rewiring that should be done to use a qualified statement in its place.
message PredicateMapping {
  // The predicate that is being replaced by the fully qualified representation of predicate + subject and object qualifiers.  Only to be used in test data and mapping data to help with the transition to the fully qualified predicate model. Not to be used in knowledge graphs.
  string mapped_predicate = 1;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 2;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 3;
  // A qualifier that composes with a core subject/object concept to define a specific type, variant, alternative version of this concept. The composed concept remains a subtype or instance of the core concept. For example, the qualifier ‘mutation’ combines with the core concept ‘Gene X’ to express the compose concept ‘a mutation of Gene X’.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_form_or_variant_qualifier = 4;
  // defines a specific part/component of the core concept (used in cases there this specific part has no IRI we can use to directly represent it, e.g. 'ESR1 transcript' q: polyA tail).  This qualifier is for the  subject of an association (or statement).
  string subject_part_qualifier = 5;
  // A qualifier that composes with a core subject/object  concept to describe something that is derived from the core concept.  For example, the qualifier ‘metabolite’ combines with a ‘Chemical X’ core concept to express the composed concept ‘a metabolite of Chemical X’.  This qualifier is for the subject of an association  (or statement).
  string subject_derivative_qualifier = 6;
  string subject_context_qualifier = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 9;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 10;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 11;
  // A qualifier that composes with a core subject/object concept to define a specific type, variant, alternative version of this concept. The composed concept remains a subtype or instance of the core concept. For example, the qualifier ‘mutation’ combines with the core concept ‘Gene X’ to express the compose concept ‘a mutation of Gene X’.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_form_or_variant_qualifier = 12;
  // defines a specific part/component of the core concept (used in cases there this specific part has no IRI we can use to directly represent it, e.g. 'ESR1 transcript' q: polyA tail).  This qualifier is for the  object of an association (or statement).
  string object_part_qualifier = 13;
  // A qualifier that composes with a core subject/object  concept to describe something that is derived from the core concept.  For example, the qualifier ‘metabolite’ combines with a ‘Chemical X’ core concept to express the composed concept ‘a metabolite of Chemical X’.  This qualifier is for the object of an association  (or statement).
  string object_derivative_qualifier = 14;
  string object_context_qualifier = 15;
  // A statement qualifier representing a type of molecular control mechanism through which an effect of a chemical on a gene or gene product is mediated (e.g. 'agonism', 'inhibition', 'allosteric modulation', 'channel blocker')
  CausalMechanismQualifierEnum causal_mechanism_qualifier = 16;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 17;
  // A statement qualifier representing a taxonomic category of species in which a relationship expressed in an association took place.
  OrganismTaxon species_context_qualifier = 18;
  // holds between two entities that have strictly equivalent meanings, with a high degree of confidence
  repeated NamedThing exact_match = 19;
  // a list of terms from different schemas or terminology systems that have a narrower, more specific meaning. Narrower terms are typically shown as children in a hierarchy or tree.
  repeated NamedThing narrow_match = 20;
  // a list of terms from different schemas or terminology systems that have a broader, more general meaning. Broader terms are typically shown as parents in a hierarchy or tree.
  repeated NamedThing broad_match = 21;
}
// a document reresenting an early version of an author's original scholarly work, such as a research paper or a review, prior to formal peer review and publication in a peer-reviewed scholarly or scientific journal.
message PreprintPublication {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // a long-form human readable name for a thing
  string full_name = 7;
  // Alternate human-readable names for a thing
  repeated string synonym = 8;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 9;
  string license = 10;
  string rights = 11;
  string format = 12;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 13;
  // connects an publication to the list of authors who contributed to the publication. This property should be a comma-delimited list of author names. It is recommended that an author's name be formatted as "surname, firstname initial.".   Note that this property is a node annotation expressing the citation list of authorship which might typically otherwise be more completely documented in biolink:PublicationToProviderAssociation defined edges which point to full details about an author and possibly, some qualifiers which clarify the specific status of a given author in the publication.
  repeated Agent authors = 14;
  // When a 2-tuple of page numbers are provided, they represent the start and end page of the publication within its parent publication context. For books, this may be set to the total number of pages of the book.
  repeated string pages = 15;
  // executive  summary of a publication
  string summary = 16;
  // keywords tagging a publication
  repeated string keywords = 17;
  // mesh terms tagging a publication
  repeated string mesh_terms = 18;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 19;
  // Ontology term for publication type may be drawn from Dublin Core types (https://www.dublincore.org/specifications/dublin-core/dcmi-type-vocabulary/), FRBR-aligned Bibliographic Ontology (https://sparontologies.github.io/fabio/current/fabio.html), the MESH publication types (https://www.nlm.nih.gov/mesh/pubtypes.html), the Confederation of Open Access Repositories (COAR) Controlled Vocabulary for Resource Type Genres (http://vocabularies.coar-repositories.org/documentation/resource_types/), Wikidata (https://www.wikidata.org/wiki/Wikidata:Publication_types), or equivalent publication type ontology. When a given publication type ontology term is used within a given knowledge graph, then the CURIE identified term must be documented in the graph as a concept node of biolink:category biolink:OntologyClass.
  repeated string publication_type = 20;
  // Different kinds of publication subtypes will have different preferred identifiers (curies when feasible). Precedence of identifiers for scientific articles is as follows: PMID if available; DOI if not; actual alternate CURIE otherwise. Enclosing publications (i.e. referenced by 'published in' node property) such as books and journals, should have industry-standard identifier such as from ISBN and ISSN.
  string id = 21;
  // the 'title' of the publication is generally recorded in the 'name' property (inherited from NamedThing). The field name 'title' is now also tagged as an acceptable alias for the node property 'name' (just in case).
  string name = 22;
}
// A series of actions conducted in a certain order or manner
message Procedure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// Describes a regulatory relationship between two genes or gene products.
message ProcessRegulatesProcessAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  BiologicalProcess subject = 37;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  BiologicalProcess object = 39;
}
// A chemical entity (often a mixture) processed for consumption for nutritional, medical or technical use. Is a material entity that is created or changed during material processing.
message ProcessedMaterial {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
  string is_supplement = 18;
  // Should be the highest level of FDA approval this chemical entity or device has, regardless of which disease, condition or phenotype it is currently being reviewed to treat.  For specific levels of FDA approval for a specific condition, disease, phenotype, etc., see the association slot, 'clinical approval status.'
  ApprovalStatusEnum highest_FDA_approval_status = 19;
  // An agglomeration of drug regulatory status worldwide. Not specific to FDA.
  ApprovalStatusEnum drug_regulatory_status_world_wide = 20;
  // the method or process of administering a pharmaceutical compound to achieve a therapeutic effect in humans or animals.
  repeated DrugDeliveryEnum routes_of_delivery = 21;
}
// A gene product that is composed of a chain of amino acid sequences and is produced by ribosome-mediated translation of mRNA
message Protein {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// A conserved part of protein sequence and (tertiary) structure that can evolve, function, and exist independently of the rest of the protein chain. Protein domains maintain their structure and function independently of the proteins in which they are found. e.g. an SH3 domain.
message ProteinDomain {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // connects an entity with one or more gene or gene products
  repeated Gene has_gene_or_gene_product = 15;
}
message ProteinFamily {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // connects an entity with one or more gene or gene products
  repeated Gene has_gene_or_gene_product = 15;
}
// Represents a protein that is a specific isoform of the canonical or reference protein. See https://www.ncbi.nlm.nih.gov/pmc/articles/PMC4114032/
message ProteinIsoform {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// Any ‘published’ piece of information. Publications are considered broadly to include any document or document part made available in print or on the web - which may include scientific journal issues, individual articles, and books - as well as things like pre-prints, white papers, patents, drug labels, web pages, protocol documents,  and even a part of a publication if of significant knowledge scope (e.g. a figure, figure legend, or section highlighted by NLP).
message Publication {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // a long-form human readable name for a thing
  string full_name = 7;
  // Alternate human-readable names for a thing
  repeated string synonym = 8;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 9;
  string license = 10;
  string rights = 11;
  string format = 12;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 13;
  // connects an publication to the list of authors who contributed to the publication. This property should be a comma-delimited list of author names. It is recommended that an author's name be formatted as "surname, firstname initial.".   Note that this property is a node annotation expressing the citation list of authorship which might typically otherwise be more completely documented in biolink:PublicationToProviderAssociation defined edges which point to full details about an author and possibly, some qualifiers which clarify the specific status of a given author in the publication.
  repeated Agent authors = 14;
  // When a 2-tuple of page numbers are provided, they represent the start and end page of the publication within its parent publication context. For books, this may be set to the total number of pages of the book.
  repeated string pages = 15;
  // executive  summary of a publication
  string summary = 16;
  // keywords tagging a publication
  repeated string keywords = 17;
  // mesh terms tagging a publication
  repeated string mesh_terms = 18;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 19;
  // Ontology term for publication type may be drawn from Dublin Core types (https://www.dublincore.org/specifications/dublin-core/dcmi-type-vocabulary/), FRBR-aligned Bibliographic Ontology (https://sparontologies.github.io/fabio/current/fabio.html), the MESH publication types (https://www.nlm.nih.gov/mesh/pubtypes.html), the Confederation of Open Access Repositories (COAR) Controlled Vocabulary for Resource Type Genres (http://vocabularies.coar-repositories.org/documentation/resource_types/), Wikidata (https://www.wikidata.org/wiki/Wikidata:Publication_types), or equivalent publication type ontology. When a given publication type ontology term is used within a given knowledge graph, then the CURIE identified term must be documented in the graph as a concept node of biolink:category biolink:OntologyClass.
  repeated string publication_type = 20;
  // Different kinds of publication subtypes will have different preferred identifiers (curies when feasible). Precedence of identifiers for scientific articles is as follows: PMID if available; DOI if not; actual alternate CURIE otherwise. Enclosing publications (i.e. referenced by 'published in' node property) such as books and journals, should have industry-standard identifier such as from ISBN and ISSN.
  string id = 21;
  // the 'title' of the publication is generally recorded in the 'name' property (inherited from NamedThing). The field name 'title' is now also tagged as an acceptable alias for the node property 'name' (just in case).
  string name = 22;
}
// A value of an attribute that is quantitative and measurable, expressed as a combination of a unit and a numeric value
message QuantityValue {
  // connects a quantity value to a unit
  string has_unit = 1;
  // connects a quantity value to a number
  double has_numeric_value = 2;
}
message ReactionToCatalystAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 37;
  // the relationship between the relative quantities of substances taking part in a reaction or forming a compound, typically a ratio of whole integers.
  int32 stoichiometry = 38;
  // the direction of a reaction as constrained by the direction enum (ie: left_to_right, neutral, etc.)
  ReactionDirectionEnum reaction_direction = 39;
  // the side of a reaction being modeled (ie: left or right)
  ReactionSideEnum reaction_side = 40;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  MolecularEntity subject = 41;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  GeneOrGeneProduct object = 42;
}
message ReactionToParticipantAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 37;
  // the chemical element that is the target of the statement
  ChemicalEntity object = 38;
  // the relationship between the relative quantities of substances taking part in a reaction or forming a compound, typically a ratio of whole integers.
  int32 stoichiometry = 39;
  // the direction of a reaction as constrained by the direction enum (ie: left_to_right, neutral, etc.)
  ReactionDirectionEnum reaction_direction = 40;
  // the side of a reaction being modeled (ie: left or right)
  ReactionSideEnum reaction_side = 41;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  MolecularEntity subject = 42;
}
// A gene altered in its expression level in the context of some experiment as a result of being targeted by gene-knockdown reagent(s) such as a morpholino or RNAi.
message ReagentTargetedGene {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 15;
}
// A region (or regions) of the genome that contains known or putative regulatory elements that act in cis- or trans- to affect the transcription of gene
message RegulatoryRegion {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 15;
}
message RelationshipQuantifier {
}
// An OWL property used as an edge label
message RelationshipType {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
}
// A result of a relative frequency analysis.
message RelativeFrequencyAnalysisResult {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// Provides information about how a particular InformationResource served as a source from which knowledge expressed in an Edge, or data used to generate this knowledge, was retrieved.
message RetrievalSource {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  string license = 12;
  string rights = 13;
  string format = 14;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 15;
  // The InformationResource that served as a source for the knowledge expressed in an Edge, or data used to generate this knowledge.
  string resource_id = 16;
  // The role of the InformationResource in the retrieval of the knowledge expressed in an Edge, or data used to generate this knowledge.
  ResourceRoleEnum resource_role = 17;
  // The InformationResources that served as a source for the InformationResource that served as a source for the knowledge expressed in an Edge, or data used to generate this knowledge.
  string upstream_resource_ids = 18;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 19;
}
message RNAProduct {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// Represents a protein that is a specific isoform of the canonical or reference RNA
message RNAProductIsoform {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
message SensitivityQuantifier {
}
// An association between a sequence feature and a nucleic acid entity it is localized to.
message SequenceAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 8;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 9;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 10;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 11;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 12;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 13;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 14;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 15;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 16;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 17;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 18;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 19;
  // a point in time
  string timepoint = 20;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 21;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 22;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 24;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 25;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 26;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 27;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 28;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 29;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 30;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 31;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 32;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 33;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 34;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 35;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 36;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 37;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 38;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 39;
}
// For example, a particular exon is part of a particular transcript or gene
message SequenceFeatureRelationship {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NucleicAcidEntity subject = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NucleicAcidEntity object = 39;
}
// A sequence_variant is a non exact copy of a sequence_feature or genome exhibiting one or more sequence_alteration.
message SequenceVariant {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  repeated string type = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // Each allele can be associated with any number of genes
  repeated Gene has_gene = 14;
  // The state of the sequence w.r.t a reference sequence
  string has_biological_sequence = 15;
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 16;
}
// An association between a sequence variant and a treatment or health intervention. The treatment object itself encompasses both the disease and the drug used.
message SequenceVariantModulatesTreatmentAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // variant that modulates the treatment of some disease
  SequenceVariant subject = 38;
  // treatment whose efficacy is modulated by the subject variant
  Treatment object = 39;
}
// This class may rarely be instantiated except if use cases of a given knowledge graph support its utility.
message Serial {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  // a human-readable description of an entity
  string description = 2;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 3;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 4;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 5;
  // a long-form human readable name for a thing
  string full_name = 6;
  // Alternate human-readable names for a thing
  repeated string synonym = 7;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 8;
  string license = 9;
  string rights = 10;
  string format = 11;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 12;
  // connects an publication to the list of authors who contributed to the publication. This property should be a comma-delimited list of author names. It is recommended that an author's name be formatted as "surname, firstname initial.".   Note that this property is a node annotation expressing the citation list of authorship which might typically otherwise be more completely documented in biolink:PublicationToProviderAssociation defined edges which point to full details about an author and possibly, some qualifiers which clarify the specific status of a given author in the publication.
  repeated Agent authors = 13;
  // When a 2-tuple of page numbers are provided, they represent the start and end page of the publication within its parent publication context. For books, this may be set to the total number of pages of the book.
  repeated string pages = 14;
  // executive  summary of a publication
  string summary = 15;
  // keywords tagging a publication
  repeated string keywords = 16;
  // mesh terms tagging a publication
  repeated string mesh_terms = 17;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 18;
  // Ontology term for publication type may be drawn from Dublin Core types (https://www.dublincore.org/specifications/dublin-core/dcmi-type-vocabulary/), FRBR-aligned Bibliographic Ontology (https://sparontologies.github.io/fabio/current/fabio.html), the MESH publication types (https://www.nlm.nih.gov/mesh/pubtypes.html), the Confederation of Open Access Repositories (COAR) Controlled Vocabulary for Resource Type Genres (http://vocabularies.coar-repositories.org/documentation/resource_types/), Wikidata (https://www.wikidata.org/wiki/Wikidata:Publication_types), or equivalent publication type ontology. When a given publication type ontology term is used within a given knowledge graph, then the CURIE identified term must be documented in the graph as a concept node of biolink:category biolink:OntologyClass.
  repeated string publication_type = 19;
  // the 'title' of the publication is generally recorded in the 'name' property (inherited from NamedThing). The field name 'title' is now also tagged as an acceptable alias for the node property 'name' (just in case).
  string name = 20;
  // Standard abbreviation for periodicals in the International Organization for Standardization (ISO) 4 system See https://www.issn.org/services/online-services/access-to-the-ltwa/. If the 'published in' property is set, then the iso abbreviation pertains to the broader publication context (the journal) within which the given publication node is embedded, not the publication itself.
  string iso_abbreviation = 21;
  // volume of a book or music release in a collection/series or a published collection of journal issues in a serial publication
  string volume = 22;
  // issue of a newspaper, a scientific journal or magazine for reference purpose
  string issue = 23;
  // Serials (journals) should have industry-standard identifier such as from ISSN.
  string id = 24;
  // Should generally be set to an ontology class defined term for 'serial' or 'journal'.
  repeated string type = 25;
}
// describes the severity of a phenotypic feature or disease
message SeverityValue {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// A small RNA molecule that is the product of a longer exogenous or endogenous dsRNA, which is either a bimolecular duplex or very long hairpin, processed (via the Dicer pathway) such that numerous siRNAs accumulate from both strands of the dsRNA. SRNAs trigger the cleavage of their target molecules.
message SiRNA {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// A small molecule entity is a molecular entity characterized by availability in small-molecule databases of SMILES, InChI, IUPAC, or other unambiguous representation of its precise chemical structure; for convenience of representation, any valid chemical representation is included, even if it is not strictly molecular (e.g., sodium ion).
message SmallMolecule {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string trade_name = 13;
  repeated DrugAvailabilityEnum available_from = 14;
  // The highest dose of a drug or treatment that does not cause unacceptable side effects. The maximum tolerated dose is determined in clinical trials by testing increasing doses on different groups of people until the highest dose with acceptable side effects is found. Also called MTD.
  string max_tolerated_dose = 15;
  bool is_toxic = 16;
  // A role is particular behaviour which a chemical entity may exhibit.
  repeated ChemicalRole has_chemical_role = 17;
  // indicates whether a molecular entity is a metabolite
  bool is_metabolite = 18;
}
// SNVs are single nucleotide positions in genomic DNA at which different sequence alternatives exist
message Snv {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  repeated string type = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // Each allele can be associated with any number of genes
  repeated Gene has_gene = 14;
  // The state of the sequence w.r.t a reference sequence
  string has_biological_sequence = 15;
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 16;
}
// Attributes relating to a socioeconomic manifestation
message SocioeconomicAttribute {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}
// A socioeconomic exposure is a factor relating to social and financial status of an affected individual (e.g. poverty).
message SocioeconomicExposure {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 4;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 5;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 6;
  // a long-form human readable name for a thing
  string full_name = 7;
  // Alternate human-readable names for a thing
  repeated string synonym = 8;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 9;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 10;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 11;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 12;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 13;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 14;
  // connects any entity to an attribute
  repeated SocioeconomicAttribute has_attribute = 15;
  // a point in time
  string timepoint = 16;
}
// An general social or economic outcome, such as healthcare costs, utilization, etc., resulting from an exposure event
message SocioeconomicOutcome {
}
message SpecificityQuantifier {
}
// a detailed investigation and/or analysis
message Study {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
}
// A group of people banded together or treated as a group as participants in a research study.
message StudyPopulation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// A collection of data items from a study that are about a particular study subject or experimental unit (the 'focus' of the Result) - optionally with context/provenance metadata that may be relevant to the interpretation of this data as evidence.
message StudyResult {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// a variable that is used as a measure in the investigation of a study
message StudyVariable {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// An entity that has the role of being studied in an investigation, study, or experiment
message SubjectOfInvestigation {
}
message TaxonToTaxonAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  OrganismTaxon subject = 38;
  // An association between individuals of different taxa.
  OrganismTaxon object = 39;
}
// A descriptor for the rank within a taxonomic classification. Example instance: TAXRANK:0000017 (kingdom)
message TaxonomicRank {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
}
// A result of text mining.
message TextMiningResult {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  string license = 13;
  string rights = 14;
  string format = 15;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 16;
}
// A mixin that can be used on any entity that can be taxonomically classified. This includes individual organisms; genes, their products and other molecular entities; body parts; biological processes
message ThingWithTaxon {
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 1;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 2;
}
// An RNA synthesized on a DNA or RNA template by an RNA polymerase.
message Transcript {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
}
// A gene is a collection of transcripts
message TranscriptToGeneRelationship {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Transcript subject = 38;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Gene object = 39;
}
// A region (or regions) of the genome that contains a region of DNA known or predicted to bind a protein that modulates gene transcription
message TranscriptionFactorBindingSite {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 13;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 14;
  // connects a genomic feature to its sequence
  string has_biological_sequence = 15;
}
// A treatment is targeted at a disease or phenotype and may involve multiple drug 'exposures', medical devices and/or procedures
message Treatment {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 6;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 7;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 8;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 9;
  // a long-form human readable name for a thing
  string full_name = 10;
  // Alternate human-readable names for a thing
  repeated string synonym = 11;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 12;
  // connects an entity to one or more drugs
  repeated Drug has_drug = 13;
  // connects an entity to one or more (medical) devices
  repeated Device has_device = 14;
  // connects an entity to one or more (medical) procedures
  repeated Procedure has_procedure = 15;
  // a point in time
  string timepoint = 16;
}
message VariantAsAModelOfDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // E.g. is pathogenic for
  string predicate = 37;
  // a disease that is associated with that variant
  NamedThing object = 38;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 39;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 40;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 41;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 42;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 43;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 44;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 45;
  // A variant that has a role in modeling the disease.
  SequenceVariant subject = 46;
}
message VariantToDiseaseAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 7;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 8;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 9;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 10;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 11;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 12;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 13;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 14;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 15;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 16;
  // a point in time
  string timepoint = 17;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 18;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 19;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 20;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 22;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 23;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 24;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 25;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 26;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 27;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 28;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 29;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 30;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 31;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 32;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 33;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 34;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 35;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 36;
  // a sequence variant in which the allele state is associated in some way with the disease state
  NamedThing subject = 37;
  // E.g. is pathogenic for
  string predicate = 38;
  // a disease that is associated with that variant
  NamedThing object = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 46;
}
message VariantToEntityAssociationMixin {
  // a sequence variant in which the allele state is associated with some other entity
  SequenceVariant subject = 1;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 2;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 3;
}
// An association between a variant and a gene, where the variant has a genetic association with the gene (i.e. is in linkage disequilibrium)
message VariantToGeneAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Gene object = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
}
// An association between a variant and expression of a gene (i.e. e-QTL)
message VariantToGeneExpressionAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // connects an association to the subject of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing subject = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  Gene object = 38;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 39;
  // Optional quantitative value indicating degree of expression.
  OntologyClass quantifier_qualifier = 40;
  // location in which gene or protein expression takes place. May be cell, tissue, or organ.
  AnatomicalEntity expression_site = 41;
  // stage during which gene or protein expression of takes place.
  LifeStage stage_qualifier = 42;
  // in experiments (e.g. gene expression) assaying diseased or unhealthy tissue, the phenotypic state can be put here, e.g. MONDO ID. For healthy tissues, use XXX.
  DiseaseOrPhenotypicFeature phenotypic_state = 43;
}
message VariantToPhenotypicFeatureAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // connects an association to the object of the association. For example, in a gene-to-phenotype association, the gene is subject and phenotype is object.
  NamedThing object = 8;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 9;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 10;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 11;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 12;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 13;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 14;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 15;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 16;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 17;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 18;
  // a point in time
  string timepoint = 19;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 20;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 21;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 23;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 24;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 25;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 26;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 27;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 28;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 29;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 30;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 31;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 32;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 33;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 34;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 35;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 36;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 37;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 38;
  // a sequence variant in which the allele state is associated in some way with the phenotype state
  SequenceVariant subject = 39;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 40;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the subject of an association (aka: statement).
  string subject_aspect_qualifier = 41;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the subject of an association (aka: statement).
  DirectionQualifierEnum subject_direction_qualifier = 42;
  // Composes with the core concept to describe new concepts of a different ontological type. e.g. a process in which the core concept participates, a function/activity/role held by the core concept, or a characteristic/quality that inheres in the core concept.  The purpose of the aspect slot is to indicate what aspect is being affected in an 'affects' association.  This qualifier specifies a change in the object of an association (aka: statement).
  string object_aspect_qualifier = 43;
  // Composes with the core concept (+ aspect if provided) to describe a change in its direction or degree. This qualifier qualifies the object of an association (aka: statement).
  DirectionQualifierEnum object_direction_qualifier = 44;
  // Predicate to be used in an association when subject and object qualifiers are present and the full reading of the statement requires a qualification to the predicate in use in order to refine or increase the specificity of the full statement reading.  This qualifier holds a relationship to be used instead of that expressed by the primary predicate, in a ‘full statement’ reading of the association, where qualifier-based semantics are included.  This is necessary only in cases where the primary predicate does not work in a full statement reading.
  string qualified_predicate = 45;
  // a qualifier used in a phenotypic association to state whether the association is specific to a particular sex.
  BiologicalSex sex_qualifier = 46;
  // A context qualifier representing a disease or condition in which a relationship expressed in an association took place.
  Disease disease_context_qualifier = 47;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string subject_specialization_qualifier = 48;
  // A qualifier that composes with a core subject/object concept to define a more specific version of the  subject concept, specifically using an ontology term that is not a subclass or descendant of the core concept  and in the vast majority of cases, is of a different ontological namespace than the category or namespace of the subject identifier.
  string object_specialization_qualifier = 49;
  // A statement qualifier representing an anatomical location where an relationship expressed in an association took place (can be a tissue, cell type, or sub-cellular location).
  string anatomical_context_qualifier = 50;
  // number of things with a particular property
  int32 has_count = 51;
  // total number of things in a particular reference set
  int32 has_total = 52;
  double has_quotient = 53;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 54;
}
// An association between a variant and a population, where the variant has particular frequency in the population
message VariantToPopulationAssociation {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  // A human-readable name for an attribute or entity.
  string name = 3;
  // a human-readable description of an entity
  string description = 4;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // A high-level grouping for the relationship type. AKA minimal predicate. This is analogous to category for nodes.
  string predicate = 7;
  // if set to true, then the association is negated i.e. is not true
  bool negated = 8;
  // grouping slot for all qualifiers on an edge.  useful for testing compliance with association classes
  string qualifier = 9;
  // connects an association to qualifiers that modify or qualify the meaning of that association
  repeated OntologyClass qualifiers = 10;
  // One or more publications that report the statement expressed in an Association, or provide information used as evidence supporting this statement.
  repeated Publication publications = 11;
  // connects an association to an instance of supporting evidence
  repeated EvidenceType has_evidence = 12;
  // An Information Resource from which the knowledge expressed in an Association was retrieved, directly or indirectly. This can be any resource through which the knowledge passed on its way to its currently serialized form. In practice, implementers should use one of the more specific subtypes of this generic property.
  string knowledge_source = 13;
  // The most upstream source of the knowledge expressed in an Association that an implementer can identify.  Performing a rigorous analysis of upstream data providers is expected; every effort is made to catalog the most upstream source of data in this property.  Only one data source should be declared primary in any association.  "aggregator knowledge source" can be used to capture non-primary sources.
  string primary_knowledge_source = 14;
  // An intermediate aggregator resource from which knowledge expressed in an Association was retrieved downstream of the original source, on its path to its current serialized form.
  repeated string aggregator_knowledge_source = 15;
  // Describes the level of knowledge expressed in a statement, based on the reasoning or analysis methods used to generate the statement, or the scope or specificity of what the statement expresses to be true.
  KnowledgeLevelEnum knowledge_level = 16;
  // Describes the high-level category of agent who originally generated a  statement of knowledge or other type of information.
  AgentTypeEnum agent_type = 17;
  // a point in time
  string timepoint = 18;
  // used to hold the original subject of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_subject = 19;
  // used to hold the original relation/predicate that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_predicate = 20;
  // used to hold the original object of a relation (or predicate) that an external knowledge source uses before transformation to match the biolink-model specification.
  string original_object = 21;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass subject_category = 22;
  // Used to hold the biolink class/category of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  OntologyClass object_category = 23;
  // Used to hold the subject closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_closure = 24;
  // Used to hold the object closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_closure = 25;
  // Used to hold the subject category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass subject_category_closure = 26;
  // Used to hold the object category closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated OntologyClass object_category_closure = 27;
  // Used to hold the subject namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string subject_namespace = 28;
  // Used to hold the object namespace of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  string object_namespace = 29;
  // Used to hold the subject label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string subject_label_closure = 30;
  // Used to hold the object label closure of an association. This is a denormalized field used primarily in the SQL serialization of a knowledge graph via KGX.
  repeated string object_label_closure = 31;
  // A list of retrieval sources that served as a source of knowledge expressed in an Edge, or a source of data used to generate this knowledge.
  repeated RetrievalSource retrieval_source_ids = 32;
  // A quantitative confidence value that represents the probability of obtaining a result at least as extreme as that actually obtained, assuming that the actual value was the result of chance alone.
  float p_value = 33;
  // The adjusted p-value is the probability of obtaining test results at least as extreme as the results actually observed, under the assumption that the null hypothesis is correct, adjusted for multiple comparisons. P is always italicized and capitalized. The actual P value* should be expressed (P=. 04) rather than expressing a statement of inequality (P<. 05), unless P<.
  float adjusted_p_value = 34;
  // A study that produced information used as evidence to generate the knowledge expressed in an Association.
  repeated Study has_supporting_studies = 35;
  // rdf:type of biolink:Association should be fixed at rdf:Statement
  repeated string type = 36;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 37;
  // an allele that has a certain frequency in a given population
  SequenceVariant subject = 38;
  // the population that is observed to have the frequency
  PopulationOfIndividualOrganisms object = 39;
  // frequency of allele in population, expressed as a number with allele divided by number in reference population, aka allele frequency
  double has_quotient = 40;
  // number in object population that carry a particular allele, aka allele count
  int32 has_count = 41;
  // number all populations that carry a particular allele, aka allele number
  int32 has_total = 42;
  // equivalent to has quotient multiplied by 100
  double has_percentage = 43;
  // a qualifier used in a phenotypic association to state how frequent the phenotype is observed in the subject
  string frequency_qualifier = 44;
}
// A sub-phylum of animals consisting of those having a bony or cartilaginous vertebral column.
message Vertebrate {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// A virus is a microorganism that replicates itself as a microRNA and infects the host cell.
message Virus {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 2;
  repeated string type = 3;
  // A human-readable name for an attribute or entity.
  string name = 4;
  // a human-readable description of an entity
  string description = 5;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 6;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 7;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 8;
  // a long-form human readable name for a thing
  string full_name = 9;
  // Alternate human-readable names for a thing
  repeated string synonym = 10;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 11;
  // connects an entity to its taxonomic classification. Only certain kinds of entities can be taxonomically classified; see 'thing with taxon'
  repeated OrganismTaxon in_taxon = 12;
  // The human readable scientific name for the taxon of the entity.
  string in_taxon_label = 13;
  // may often be an organism attribute
  repeated Attribute has_attribute = 14;
}
// a document that is published according to World Wide Web standards, which may incorporate text, graphics, sound, and/or other features.
message WebPage {
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // a long-form human readable name for a thing
  string full_name = 7;
  // Alternate human-readable names for a thing
  repeated string synonym = 8;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 9;
  string license = 10;
  string rights = 11;
  string format = 12;
  // date on which an entity was created. This can be applied to nodes or edges
  string creation_date = 13;
  // connects an publication to the list of authors who contributed to the publication. This property should be a comma-delimited list of author names. It is recommended that an author's name be formatted as "surname, firstname initial.".   Note that this property is a node annotation expressing the citation list of authorship which might typically otherwise be more completely documented in biolink:PublicationToProviderAssociation defined edges which point to full details about an author and possibly, some qualifiers which clarify the specific status of a given author in the publication.
  repeated Agent authors = 14;
  // When a 2-tuple of page numbers are provided, they represent the start and end page of the publication within its parent publication context. For books, this may be set to the total number of pages of the book.
  repeated string pages = 15;
  // executive  summary of a publication
  string summary = 16;
  // keywords tagging a publication
  repeated string keywords = 17;
  // mesh terms tagging a publication
  repeated string mesh_terms = 18;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 19;
  // Ontology term for publication type may be drawn from Dublin Core types (https://www.dublincore.org/specifications/dublin-core/dcmi-type-vocabulary/), FRBR-aligned Bibliographic Ontology (https://sparontologies.github.io/fabio/current/fabio.html), the MESH publication types (https://www.nlm.nih.gov/mesh/pubtypes.html), the Confederation of Open Access Repositories (COAR) Controlled Vocabulary for Resource Type Genres (http://vocabularies.coar-repositories.org/documentation/resource_types/), Wikidata (https://www.wikidata.org/wiki/Wikidata:Publication_types), or equivalent publication type ontology. When a given publication type ontology term is used within a given knowledge graph, then the CURIE identified term must be documented in the graph as a concept node of biolink:category biolink:OntologyClass.
  repeated string publication_type = 20;
  // Different kinds of publication subtypes will have different preferred identifiers (curies when feasible). Precedence of identifiers for scientific articles is as follows: PMID if available; DOI if not; actual alternate CURIE otherwise. Enclosing publications (i.e. referenced by 'published in' node property) such as books and journals, should have industry-standard identifier such as from ISBN and ISSN.
  string id = 21;
  // the 'title' of the publication is generally recorded in the 'name' property (inherited from NamedThing). The field name 'title' is now also tagged as an acceptable alias for the node property 'name' (just in case).
  string name = 22;
}
message Zygosity {
  // A unique identifier for an entity. Must be either a CURIE shorthand for a URI or a complete URI
  string id = 1;
  repeated string type = 2;
  // a human-readable description of an entity
  string description = 3;
  // connects any entity to an attribute
  repeated Attribute has_attribute = 4;
  // A boolean flag indicating that an entity is no longer considered current or valid.
  bool deprecated = 5;
  // The value in this node property represents the knowledge provider that created or assembled the node and all of its attributes.  Used internally to represent how a particular node made its way into a knowledge provider or graph.
  repeated string provided_by = 6;
  // A database cross reference or alternative identifier for a NamedThing or edge between two NamedThings.  This property should point to a database record or webpage that supports the existence of the edge, or gives more detail about the edge. This property can be used on a node or edge to provide multiple URIs or CURIE cross references.
  repeated string xref = 7;
  // a long-form human readable name for a thing
  string full_name = 8;
  // Alternate human-readable names for a thing
  repeated string synonym = 9;
  // Name of the high level ontology class in which this entity is categorized. Corresponds to the label for the biolink entity type class. In a neo4j database this MAY correspond to the neo4j label tag. In an RDF database it should be a biolink model class URI. This field is multi-valued. It should include values for ancestors of the biolink class; for example, a protein such as Shh would have category values `biolink:Protein`, `biolink:GeneProduct`, `biolink:MolecularEntity`. In an RDF database, nodes will typically have an rdf:type triples. This can be to the most specific biolink class, or potentially to a class more specific than something in biolink. For example, a sequence feature `f` may have a rdf:type assertion to a SO class such as TF_binding_site, which is more specific than anything in biolink. Here we would have categories {biolink:GenomicEntity, biolink:MolecularEntity, biolink:NamedThing}
  repeated string category = 10;
  // The human-readable 'attribute name' can be set to a string which reflects its context of interpretation, e.g. SEPIO evidence/provenance/confidence annotation or it can default to the name associated with the 'has attribute type' slot ontology term.
  string name = 11;
  // connects an attribute to a class that describes it
  OntologyClass has_attribute_type = 12;
  // connects an attribute to a value
  repeated QuantityValue has_quantitative_value = 13;
  // connects an attribute to a value
  NamedThing has_qualitative_value = 14;
  // An IRI for an entity. This is determined by the id using expansion rules.
  string iri = 15;
}

enum AgentTypeEnum {
  AGENT_TYPE_ENUM_UNSPECIFIED = 0;
  AGENT_TYPE_ENUM_MANUAL_AGENT = 1;
  AGENT_TYPE_ENUM_AUTOMATED_AGENT = 2;
  AGENT_TYPE_ENUM_DATA_ANALYSIS_PIPELINE = 3;
  AGENT_TYPE_ENUM_COMPUTATIONAL_MODEL = 4;
  AGENT_TYPE_ENUM_TEXT_MINING_AGENT = 5;
  AGENT_TYPE_ENUM_IMAGE_PROCESSING_AGENT = 6;
  AGENT_TYPE_ENUM_MANUAL_VALIDATION_OF_AUTOMATED_AGENT = 7;
  AGENT_TYPE_ENUM_NOT_PROVIDED = 8;
}
enum ApprovalStatusEnum {
  APPROVAL_STATUS_ENUM_UNSPECIFIED = 0;
  APPROVAL_STATUS_ENUM_DISCOVERY_AND_DEVELOPMENT_PHASE = 1;
  APPROVAL_STATUS_ENUM_PRECLINICAL_RESEARCH_PHASE = 2;
  APPROVAL_STATUS_ENUM_FDA_CLINICAL_RESEARCH_PHASE = 3;
  APPROVAL_STATUS_ENUM_FDA_REVIEW_PHASE_N4 = 4;
  APPROVAL_STATUS_ENUM_FDA_POST_MARKET_SAFETY_REVIEW = 5;
  APPROVAL_STATUS_ENUM_FDA_CLINICAL_RESEARCH_PHASE_N1 = 6;
  APPROVAL_STATUS_ENUM_FDA_CLINICAL_RESEARCH_PHASE_N2 = 7;
  APPROVAL_STATUS_ENUM_FDA_CLINICAL_RESEARCH_PHASE_N3 = 8;
  APPROVAL_STATUS_ENUM_FDA_CLINICAL_RESEARCH_PHASE_N4 = 9;
  APPROVAL_STATUS_ENUM_FDA_FAST_TRACK = 10;
  APPROVAL_STATUS_ENUM_FDA_BREAKTHROUGH_THERAPY = 11;
  APPROVAL_STATUS_ENUM_FDA_ACCELERATED_APPROVAL = 12;
  APPROVAL_STATUS_ENUM_FDA_PRIORITY_REVIEW = 13;
  APPROVAL_STATUS_ENUM_REGULAR_FDA_APPROVAL = 14;
  APPROVAL_STATUS_ENUM_POST_APPROVAL_WITHDRAWAL = 15;
}
enum CausalMechanismQualifierEnum {
  CAUSAL_MECHANISM_QUALIFIER_ENUM_UNSPECIFIED = 0;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_BINDING = 1;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_INHIBITION = 2;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_ANTIBODY_INHIBITION = 3;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_ANTAGONISM = 4;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_MOLECULAR_CHANNEL_BLOCKAGE = 5;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_INVERSE_AGONISM = 6;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_NEGATIVE_ALLOSTERIC_MODULATION = 7;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_AGONISM = 8;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_MOLECULAR_CHANNEL_OPENING = 9;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_POSITIVE_ALLOSTERIC_MODULATION = 10;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_POTENTIATION = 11;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_ACTIVATION = 12;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_INDUCER = 13;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_TRANSCRIPTIONAL_REGULATION = 14;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_SIGNALING_MEDIATED_CONTROL = 15;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_STABILIZATION = 16;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_STIMULATION = 17;
  CAUSAL_MECHANISM_QUALIFIER_ENUM_RELEASING_ACTIVITY = 18;
}
enum ChemicalEntityDerivativeEnum {
  CHEMICAL_ENTITY_DERIVATIVE_ENUM_UNSPECIFIED = 0;
  CHEMICAL_ENTITY_DERIVATIVE_ENUM_METABOLITE = 1;
}
enum ChemicalOrGeneOrGeneProductFormOrVariantEnum {
  CHEMICAL_OR_GENE_OR_GENE_PRODUCT_FORM_OR_VARIANT_ENUM_UNSPECIFIED = 0;
  CHEMICAL_OR_GENE_OR_GENE_PRODUCT_FORM_OR_VARIANT_ENUM_GENETIC_VARIANT_FORM = 1;
  CHEMICAL_OR_GENE_OR_GENE_PRODUCT_FORM_OR_VARIANT_ENUM_MODIFIED_FORM = 2;
  CHEMICAL_OR_GENE_OR_GENE_PRODUCT_FORM_OR_VARIANT_ENUM_LOSS_OF_FUNCTION_VARIANT_FORM = 3;
  CHEMICAL_OR_GENE_OR_GENE_PRODUCT_FORM_OR_VARIANT_ENUM_GAIN_OF_FUNCTION_VARIANT_FORM = 4;
  CHEMICAL_OR_GENE_OR_GENE_PRODUCT_FORM_OR_VARIANT_ENUM_POLYMORPHIC_FORM = 5;
  CHEMICAL_OR_GENE_OR_GENE_PRODUCT_FORM_OR_VARIANT_ENUM_SNP_FORM = 6;
  CHEMICAL_OR_GENE_OR_GENE_PRODUCT_FORM_OR_VARIANT_ENUM_ANALOG_FORM = 7;
}
enum ClinicalApprovalStatusEnum {
  CLINICAL_APPROVAL_STATUS_ENUM_UNSPECIFIED = 0;
  CLINICAL_APPROVAL_STATUS_ENUM_APPROVED_FOR_CONDITION = 1;
  CLINICAL_APPROVAL_STATUS_ENUM_FDA_APPROVED_FOR_CONDITION = 2;
  CLINICAL_APPROVAL_STATUS_ENUM_NOT_APPROVED_FOR_CONDITION = 3;
  CLINICAL_APPROVAL_STATUS_ENUM_POST_APPROVAL_WITHDRAWAL = 4;
  CLINICAL_APPROVAL_STATUS_ENUM_OFF_LABEL_USE = 5;
  CLINICAL_APPROVAL_STATUS_ENUM_NOT_PROVIDED = 6;
}
enum DirectionQualifierEnum {
  DIRECTION_QUALIFIER_ENUM_UNSPECIFIED = 0;
  DIRECTION_QUALIFIER_ENUM_INCREASED = 1;
  DIRECTION_QUALIFIER_ENUM_UPREGULATED = 2;
  DIRECTION_QUALIFIER_ENUM_DECREASED = 3;
  DIRECTION_QUALIFIER_ENUM_DOWNREGULATED = 4;
}
enum DrugAvailabilityEnum {
  DRUG_AVAILABILITY_ENUM_UNSPECIFIED = 0;
  DRUG_AVAILABILITY_ENUM_OVER_THE_COUNTER = 1;
  DRUG_AVAILABILITY_ENUM_PRESCRIPTION = 2;
}
enum DrugDeliveryEnum {
  DRUG_DELIVERY_ENUM_UNSPECIFIED = 0;
  DRUG_DELIVERY_ENUM_INHALATION = 1;
  DRUG_DELIVERY_ENUM_ORAL = 2;
  DRUG_DELIVERY_ENUM_ABSORPTION_THROUGH_THE_SKIN = 3;
  DRUG_DELIVERY_ENUM_INTRAVENOUS_INJECTION = 4;
}
enum DruggableGeneCategoryEnum {
  DRUGGABLE_GENE_CATEGORY_ENUM_UNSPECIFIED = 0;
  DRUGGABLE_GENE_CATEGORY_ENUM_TCLIN = 1;
  DRUGGABLE_GENE_CATEGORY_ENUM_TBIO = 2;
  DRUGGABLE_GENE_CATEGORY_ENUM_TCHEM = 3;
  DRUGGABLE_GENE_CATEGORY_ENUM_TDARK = 4;
}
// please consult with the FDA guidelines as proposed in this document: https://www.accessdata.fda.gov/scripts/cdrh/cfdocs/cfcfr/cfrsearch.cfm?fr=312.32
enum FDAIDAAdverseEventEnum {
  FDAIDAADVERSE_EVENT_ENUM_UNSPECIFIED = 0;
  FDAIDAADVERSE_EVENT_ENUM_LIFE_THREATENING_ADVERSE_EVENT = 1;
  FDAIDAADVERSE_EVENT_ENUM_SERIOUS_ADVERSE_EVENT = 2;
  FDAIDAADVERSE_EVENT_ENUM_SUSPECTED_ADVERSE_REACTION = 3;
  FDAIDAADVERSE_EVENT_ENUM_UNEXPECTED_ADVERSE_EVENT = 4;
}
enum GeneOrGeneProductOrChemicalEntityAspectEnum {
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_UNSPECIFIED = 0;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_ACTIVITY_OR_ABUNDANCE = 1;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_ABUNDANCE = 2;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_ACTIVITY = 3;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_EXPRESSION = 4;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_SYNTHESIS = 5;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_DEGRADATION = 6;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_CLEAVAGE = 7;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_HYDROLYSIS = 8;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_METABOLIC_PROCESSING = 9;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_MUTATION_RATE = 10;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_STABILITY = 11;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_FOLDING = 12;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_LOCALIZATION = 13;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_TRANSPORT = 14;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_ABSORPTION = 15;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_AGGREGATION = 16;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_INTERACTION = 17;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_RELEASE = 18;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_SECRETION = 19;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_UPTAKE = 20;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_SPLICING = 21;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_MOLECULAR_INTERACTION = 22;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_MOLECULAR_MODIFICATION = 23;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_ACETYLATION = 24;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_ACYLATION = 25;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_ALKYLATION = 26;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_AMINATION = 27;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_CARBAMOYLATION = 28;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_ETHYLATION = 29;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_GLUTATHIONYLATION = 30;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_GLYCATION = 31;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_GLYCOSYLATION = 32;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_GLUCURONIDATION = 33;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_N_LINKED_GLYCOSYLATION = 34;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_O_LINKED_GLYCOSYLATION = 35;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_HYDROXYLATION = 36;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_LIPIDATION = 37;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_FARNESYLATION = 38;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_GERANOYLATION = 39;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_MYRISTOYLATION = 40;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_PALMITOYLATION = 41;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_PRENYLATION = 42;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_METHYLATION = 43;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_NITROSATION = 44;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_NUCLEOTIDYLATION = 45;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_PHOSPHORYLATION = 46;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_RIBOSYLATION = 47;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_ADP_RIBOSYLATION = 48;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_SULFATION = 49;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_SUMOYLATION = 50;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_UBIQUITINATION = 51;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_OXIDATION = 52;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_REDUCTION = 53;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_ENTITY_ASPECT_ENUM_CARBOXYLATION = 54;
}
enum GeneOrGeneProductOrChemicalPartQualifierEnum {
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_PART_QUALIFIER_ENUM_UNSPECIFIED = 0;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_PART_QUALIFIER_ENUM_N3_PRIME_UTR = 1;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_PART_QUALIFIER_ENUM_N5_PRIME_UTR = 2;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_PART_QUALIFIER_ENUM_POLYA_TAIL = 3;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_PART_QUALIFIER_ENUM_PROMOTER = 4;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_PART_QUALIFIER_ENUM_ENHANCER = 5;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_PART_QUALIFIER_ENUM_EXON = 6;
  GENE_OR_GENE_PRODUCT_OR_CHEMICAL_PART_QUALIFIER_ENUM_INTRON = 7;
}
enum KnowledgeLevelEnum {
  KNOWLEDGE_LEVEL_ENUM_UNSPECIFIED = 0;
  KNOWLEDGE_LEVEL_ENUM_KNOWLEDGE_ASSERTION = 1;
  KNOWLEDGE_LEVEL_ENUM_LOGICAL_ENTAILMENT = 2;
  KNOWLEDGE_LEVEL_ENUM_PREDICTION = 3;
  KNOWLEDGE_LEVEL_ENUM_STATISTICAL_ASSOCIATION = 4;
  KNOWLEDGE_LEVEL_ENUM_OBSERVATION = 5;
  KNOWLEDGE_LEVEL_ENUM_NOT_PROVIDED = 6;
}
enum LogicalInterpretationEnum {
  LOGICAL_INTERPRETATION_ENUM_UNSPECIFIED = 0;
  LOGICAL_INTERPRETATION_ENUM_SOME_SOME = 1;
  LOGICAL_INTERPRETATION_ENUM_ALL_SOME = 2;
  LOGICAL_INTERPRETATION_ENUM_INVERSE_ALL_SOME = 3;
}
// phase
enum PhaseEnum {
  PHASE_ENUM_UNSPECIFIED = 0;
  PHASE_ENUM_N0 = 1;
  PHASE_ENUM_N1 = 2;
  PHASE_ENUM_N2 = 3;
}
enum ReactionDirectionEnum {
  REACTION_DIRECTION_ENUM_UNSPECIFIED = 0;
  REACTION_DIRECTION_ENUM_LEFT_TO_RIGHT = 1;
  REACTION_DIRECTION_ENUM_RIGHT_TO_LEFT = 2;
  REACTION_DIRECTION_ENUM_BIDIRECTIONAL = 3;
  REACTION_DIRECTION_ENUM_NEUTRAL = 4;
}
enum ReactionSideEnum {
  REACTION_SIDE_ENUM_UNSPECIFIED = 0;
  REACTION_SIDE_ENUM_LEFT = 1;
  REACTION_SIDE_ENUM_RIGHT = 2;
}
enum ResearchPhaseEnum {
  RESEARCH_PHASE_ENUM_UNSPECIFIED = 0;
  RESEARCH_PHASE_ENUM_PRE_CLINICAL_RESEARCH_PHASE = 1;
  RESEARCH_PHASE_ENUM_CLINICAL_TRIAL_PHASE = 2;
  RESEARCH_PHASE_ENUM_CLINICAL_TRIAL_PHASE_N1 = 3;
  RESEARCH_PHASE_ENUM_CLINICAL_TRIAL_PHASE_N2 = 4;
  RESEARCH_PHASE_ENUM_CLINICAL_TRIAL_PHASE_N3 = 5;
  RESEARCH_PHASE_ENUM_CLINICAL_TRIAL_PHASE_N4 = 6;
  RESEARCH_PHASE_ENUM_NOT_PROVIDED = 7;
}
// The role played by the information reource in serving as a source for an edge in a TRAPI message. Note that a given Edge should have one and only one 'primary' source, and may have any number of 'aggregator' or 'supporting data' sources.  This enumeration is found in Biolink Model, but is repeated here for convenience.
enum ResourceRoleEnum {
  RESOURCE_ROLE_ENUM_UNSPECIFIED = 0;
  RESOURCE_ROLE_ENUM_PRIMARY_KNOWLEDGE_SOURCE = 1;
  RESOURCE_ROLE_ENUM_AGGREGATOR_KNOWLEDGE_SOURCE = 2;
  RESOURCE_ROLE_ENUM_SUPPORTING_DATA_SOURCE = 3;
}
// type of sequence
enum SequenceEnum {
  SEQUENCE_ENUM_UNSPECIFIED = 0;
  SEQUENCE_ENUM_NA = 1;
  SEQUENCE_ENUM_AA = 2;
}
// strand
enum StrandEnum {
  STRAND_ENUM_UNSPECIFIED = 0;
  STRAND_ENUM_N = 1;
  STRAND_ENUM_N_V2 = 2;
  STRAND_ENUM_N_V3 = 3;
  STRAND_ENUM_N_V4 = 4;
}
