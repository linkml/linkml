# Weekly compatibility test against the latest linkml-model metamodel
#
# This workflow downloads the latest metamodel from linkml-model and runs
# the compatibility tests to ensure the linkml toolkit can process it.
# If tests pass and the metamodel has changed, it creates a PR with the update.
#
# It also checks whether the vendored linkml-model files have drifted from
# upstream main, and opens (or updates) a tracking issue when drift is found.

name: Metamodel Compatibility
env:
  UV_VERSION: "0.11.21"
on:
  schedule:
    # Run every Monday at 9:00 UTC
    - cron: "0 9 * * 1"
  workflow_dispatch:

jobs:
  metamodel-compat:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v9.0.0
        with:
          version: ${{ env.UV_VERSION }}
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Setup Graphviz
        uses: ts-graphviz/setup-graphviz@v2.0.2

      - name: Download latest metamodel from linkml-model
        run: make download-metamodel

      - name: Run metamodel compatibility tests
        id: run_tests
        run: |
          mkdir -p temp
          uv run pytest tests/linkml/test_metamodel_compat/ --with-slow -v 2>&1 | tee temp/test_output.txt
        continue-on-error: true

      - name: Check for existing issue
        if: steps.run_tests.outcome == 'failure'
        id: check_issue
        run: |
          EXISTING_ISSUE=$(gh issue list --label "metamodel-compat" --state open --json number --jq '.[0].number // empty')
          echo "existing_issue=${EXISTING_ISSUE}" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create or update issue on failure
        if: steps.run_tests.outcome == 'failure'
        run: |
          ISSUE_TITLE="Metamodel compatibility test failure"

          # Build the body in a file rather than a shell variable: the test output is
          # untrusted text that must not be re-evaluated by the shell.
          {
            cat <<EOF
          ## Metamodel Compatibility Test Failure

          The weekly metamodel compatibility test has failed. This indicates that the
          current linkml toolkit may not be compatible with the latest linkml-model metamodel.

          **Workflow run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

          ### Test Output (last 100 lines)
          EOF
            echo '```'
            tail -n 100 temp/test_output.txt
            echo '```'
            cat <<'EOF'

          ### Next Steps
          1. Review the test output above
          2. Check for recent changes in [linkml-model](https://github.com/linkml/linkml-model)
          3. Update the linkml toolkit to handle any metamodel changes
          EOF
          } > temp/issue_body.md

          if [ -n "${{ steps.check_issue.outputs.existing_issue }}" ]; then
            gh issue comment "${{ steps.check_issue.outputs.existing_issue }}" --body-file temp/issue_body.md
          else
            gh issue create \
              --title "${ISSUE_TITLE}" \
              --body-file temp/issue_body.md \
              --label "metamodel-compat"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Fail if tests failed
        if: steps.run_tests.outcome == 'failure'
        run: exit 1

      - name: Create PR with updated metamodel
        if: steps.run_tests.outcome == 'success'
        uses: peter-evans/create-pull-request@v8
        with:
          commit-message: "Update metamodel test fixtures from linkml-model"
          title: "Update metamodel test fixtures from linkml-model"
          body: |
            This PR updates the metamodel test fixtures to the latest version from linkml-model.

            The metamodel compatibility tests have passed with these changes.

            **Workflow run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          branch: update-metamodel-fixtures
          delete-branch: true
          labels: metamodel-compat

      # Only the soft upstream-main comparison runs here. Scoping to that single test
      # keeps an unrelated failure -- a hard UPSTREAM_SHA mismatch, a collection error --
      # from being reported as upstream drift.
      #
      # pytest exit 1 means "tests failed", i.e. genuine drift. Anything else (2 interrupted,
      # 3 internal error, 4 usage error, or a network blip killing the run) is a broken job,
      # not drift, so it fails loudly instead of filing a misleading issue.
      - name: Check vendored files for drift against upstream main
        id: drift_tests
        if: always()
        run: |
          mkdir -p temp
          set +e
          uv run pytest tests/linkml_runtime/test_linkml_model/test_linkml_files.py \
            -k test_vendored_files_match_upstream_main \
            --with-network --with-upstream-main -v 2>&1 | tee temp/drift_output.txt
          PYTEST_RC=${PIPESTATUS[0]}
          set -e
          case "$PYTEST_RC" in
            0) echo "drifted=false" >> $GITHUB_OUTPUT ;;
            1) echo "drifted=true"  >> $GITHUB_OUTPUT ;;
            *) echo "::error::Drift check did not run cleanly (pytest exit $PYTEST_RC); not reporting drift."
               exit "$PYTEST_RC" ;;
          esac

      - name: Check for existing drift issue
        if: always() && steps.drift_tests.outputs.drifted == 'true'
        id: check_drift_issue
        run: |
          EXISTING_ISSUE=$(gh issue list --label "vendored-model-drift" --state open --json number --jq '.[0].number // empty')
          echo "existing_issue=${EXISTING_ISSUE}" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create or update issue on drift
        if: always() && steps.drift_tests.outputs.drifted == 'true'
        run: |
          ISSUE_TITLE="Vendored linkml-model files have drifted from upstream main"

          {
            cat <<EOF
          ## Vendored linkml-model Drift Detected

          The vendored linkml-model files in \`packages/linkml_runtime/src/linkml_runtime/linkml_model/\`
          have drifted from the upstream \`main\` branch of [linkml-model](https://github.com/linkml/linkml-model).

          This does not block any PR, but the vendored copies will likely need updating
          **before the next release**. Run \`make update_model\` in \`packages/linkml_runtime/\`
          to re-vendor the files.

          **Workflow run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

          ### Test Output (last 50 lines)
          EOF
            echo '```'
            tail -n 50 temp/drift_output.txt
            echo '```'
          } > temp/drift_issue_body.md

          if [ -n "${{ steps.check_drift_issue.outputs.existing_issue }}" ]; then
            gh issue comment "${{ steps.check_drift_issue.outputs.existing_issue }}" --body-file temp/drift_issue_body.md
          else
            gh issue create \
              --title "${ISSUE_TITLE}" \
              --body-file temp/drift_issue_body.md \
              --label "vendored-model-drift"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Close drift issue if drift is resolved
        if: always() && steps.drift_tests.outputs.drifted == 'false'
        run: |
          EXISTING_ISSUE=$(gh issue list --label "vendored-model-drift" --state open --json number --jq '.[0].number // empty')
          if [ -n "${EXISTING_ISSUE}" ]; then
            gh issue close "${EXISTING_ISSUE}" \
              --comment "Vendored files are now in sync with upstream main. Closing."
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Nothing updates UPSTREAM_SHA automatically -- `make update_model` is run by
      # hand -- so a new linkml-model (pre)release can sit unadopted indefinitely
      # with every check still green. This notices that. Comparing against the
      # newest release rather than main is deliberate: main moves constantly, but a
      # release means someone upstream decided something was ready to take.
      #
      # Resolution is delegated to `make print_latest_model_release`, the same
      # target `make update_model` uses, so this check and the vendoring command
      # cannot disagree about what "latest" means.
      - name: Check whether the vendored pin is behind the latest release
        id: pin_check
        if: always()
        run: |
          PINNED=$(tr -d '[:space:]' < packages/linkml_runtime/src/linkml_runtime/linkml_model/UPSTREAM_SHA)
          read -r TAG LATEST < <(make -C packages/linkml_runtime -s print_latest_model_release)

          # Fail loudly rather than guessing: with no outputs set, neither the
          # open-issue nor the close-issue step below will fire.
          if [ -z "${PINNED}" ] || [ -z "${TAG}" ] || [ -z "${LATEST}" ]; then
            echo "::error::Could not resolve the vendored pin or the latest release (pinned='${PINNED}' tag='${TAG}' latest='${LATEST}')"
            exit 1
          fi

          echo "pinned=${PINNED}" >> "$GITHUB_OUTPUT"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "latest=${LATEST}" >> "$GITHUB_OUTPUT"
          if [ "${PINNED}" = "${LATEST}" ]; then
            echo "Vendored pin matches ${TAG}."
            echo "stale=false" >> "$GITHUB_OUTPUT"
          else
            echo "Vendored pin ${PINNED} is behind ${TAG} (${LATEST})."
            echo "stale=true" >> "$GITHUB_OUTPUT"
          fi

      # gh issue create fails outright on an unknown label, so create it here rather
      # than depending on someone having made it by hand. --force makes this a no-op
      # when it already exists.
      - name: Ensure the stale-pin label exists
        if: always() && steps.pin_check.outputs.stale == 'true'
        run: |
          gh label create "vendored-model-stale-pin" \
            --description "Vendored linkml-model pin is behind the latest release" \
            --color "FBCA04" \
            --force
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check for existing stale-pin issue
        if: always() && steps.pin_check.outputs.stale == 'true'
        id: check_pin_issue
        run: |
          EXISTING_ISSUE=$(gh issue list --label "vendored-model-stale-pin" --state open --json number --jq '.[0].number // empty')
          echo "existing_issue=${EXISTING_ISSUE}" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create or update issue when the pin is stale
        if: always() && steps.pin_check.outputs.stale == 'true'
        run: |
          ISSUE_TITLE="Vendored linkml-model pin is behind the latest release"
          mkdir -p temp

          {
            cat <<EOF
          ## Vendored linkml-model Pin Is Stale

          A newer linkml-model (pre)release exists than the one the vendored copy is
          pinned to. Nothing adopts it automatically.

          | | |
          | --- | --- |
          | Pinned (\`UPSTREAM_SHA\`) | \`${{ steps.pin_check.outputs.pinned }}\` |
          | Latest release | \`${{ steps.pin_check.outputs.tag }}\` |
          | Its commit | \`${{ steps.pin_check.outputs.latest }}\` |

          To adopt it, run \`make update_model\` in \`packages/linkml_runtime/\` and
          commit the result. Expect generated-artifact snapshots to need regenerating
          in the same change.

          This does not block any PR. It stays open until the pin is updated.

          **Workflow run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          EOF
          } > temp/stale_pin_issue.md

          if [ -n "${{ steps.check_pin_issue.outputs.existing_issue }}" ]; then
            gh issue comment "${{ steps.check_pin_issue.outputs.existing_issue }}" --body-file temp/stale_pin_issue.md
          else
            gh issue create \
              --title "${ISSUE_TITLE}" \
              --body-file temp/stale_pin_issue.md \
              --label "vendored-model-stale-pin"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Close stale-pin issue once the pin is current
        if: always() && steps.pin_check.outputs.stale == 'false'
        run: |
          EXISTING_ISSUE=$(gh issue list --label "vendored-model-stale-pin" --state open --json number --jq '.[0].number // empty')
          if [ -n "${EXISTING_ISSUE}" ]; then
            gh issue close "${EXISTING_ISSUE}" \
              --comment "Vendored pin now matches ${{ steps.pin_check.outputs.tag }}. Closing."
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
